# Dependencies
node_modules/
package-lock.json
yarn.lock

# Build output
dist/
*.js
*.js.map

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Solana
.config/solana/
*.json.bak
.vercel

# esbuild Vercel-bundle output (regenerated by scripts/bundle-vercel.mjs).
# api/bundled.mjs itself is NOT ignored — see the placeholder comment at
# the top of that file for why it must exist in the checked-in source
# tree (`vercel build` validates the vercel.json `functions` glob against
# the source tree before running `buildCommand`, which is what actually
# regenerates this file).
api/bundled.mjs.meta.json
