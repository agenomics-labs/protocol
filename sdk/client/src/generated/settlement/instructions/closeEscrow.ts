/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { SETTLEMENT_PROGRAM_ADDRESS } from '../programs/index.js';

export const CLOSE_ESCROW_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([139, 171, 94, 146, 191, 91, 144, 50]);

export function getCloseEscrowDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ESCROW_DISCRIMINATOR); }

export type CloseEscrowInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountClient extends string | AccountMeta<string> = string, TAccountEscrow extends string | AccountMeta<string> = string, TAccountEscrowTokenAccount extends string | AccountMeta<string> = string, TAccountClientTokenAccount extends string | AccountMeta<string> = string, TAccountTokenProgram extends string | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountClient extends string ? WritableSignerAccount<TAccountClient> & AccountSignerMeta<TAccountClient> : TAccountClient, TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountEscrowTokenAccount extends string ? WritableAccount<TAccountEscrowTokenAccount> : TAccountEscrowTokenAccount, TAccountClientTokenAccount extends string ? WritableAccount<TAccountClientTokenAccount> : TAccountClientTokenAccount, TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram, ...TRemainingAccounts]>;

export type CloseEscrowInstructionData = { discriminator: ReadonlyUint8Array;  };

export type CloseEscrowInstructionDataArgs = {  };

export function getCloseEscrowInstructionDataEncoder(): FixedSizeEncoder<CloseEscrowInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: CLOSE_ESCROW_DISCRIMINATOR }));
}

export function getCloseEscrowInstructionDataDecoder(): FixedSizeDecoder<CloseEscrowInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getCloseEscrowInstructionDataCodec(): FixedSizeCodec<CloseEscrowInstructionDataArgs, CloseEscrowInstructionData> {
    return combineCodec(getCloseEscrowInstructionDataEncoder(), getCloseEscrowInstructionDataDecoder());
}

export type CloseEscrowInput<TAccountClient extends string = string, TAccountEscrow extends string = string, TAccountEscrowTokenAccount extends string = string, TAccountClientTokenAccount extends string = string, TAccountTokenProgram extends string = string> =  {
  client: TransactionSigner<TAccountClient>;
escrow: Address<TAccountEscrow>;
/**
 * C4-OB-02: the escrow's SPL token account (ATA whose authority is the
 * `escrow` PDA). Constrained to the escrow's `token_mint` + the escrow
 * PDA as owner so a substituted account is rejected at deserialization,
 * mirroring `ExpireEscrow`'s `escrow_token_account` constraint.
 */
escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
/**
 * C4-OB-02: destination for any residual ATA balance + the reclaimed ATA
 * rent. Bound to the escrow `client` (= the signer, via the escrow's
 * `has_one = client`) so the sweep cannot be redirected.
 */
clientTokenAccount: Address<TAccountClientTokenAccount>;
tokenProgram?: Address<TAccountTokenProgram>;
}

export function getCloseEscrowInstruction<TAccountClient extends string, TAccountEscrow extends string, TAccountEscrowTokenAccount extends string, TAccountClientTokenAccount extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof SETTLEMENT_PROGRAM_ADDRESS>(input: CloseEscrowInput<TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountClientTokenAccount, TAccountTokenProgram>, config?: { programAddress?: TProgramAddress } ): CloseEscrowInstruction<TProgramAddress, TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountClientTokenAccount, TAccountTokenProgram> {
  // Program address.
const programAddress = config?.programAddress ?? SETTLEMENT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { client: { value: input.client ?? null, isWritable: true }, escrow: { value: input.escrow ?? null, isWritable: true }, escrowTokenAccount: { value: input.escrowTokenAccount ?? null, isWritable: true }, clientTokenAccount: { value: input.clientTokenAccount ?? null, isWritable: true }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Resolve default values.
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("client", accounts.client), getAccountMeta("escrow", accounts.escrow), getAccountMeta("escrowTokenAccount", accounts.escrowTokenAccount), getAccountMeta("clientTokenAccount", accounts.clientTokenAccount), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getCloseEscrowInstructionDataEncoder().encode({}), programAddress } as CloseEscrowInstruction<TProgramAddress, TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountClientTokenAccount, TAccountTokenProgram>);
}

export type ParsedCloseEscrowInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
client: TAccountMetas[0];
escrow: TAccountMetas[1];
/**
 * C4-OB-02: the escrow's SPL token account (ATA whose authority is the
 * `escrow` PDA). Constrained to the escrow's `token_mint` + the escrow
 * PDA as owner so a substituted account is rejected at deserialization,
 * mirroring `ExpireEscrow`'s `escrow_token_account` constraint.
 */
escrowTokenAccount: TAccountMetas[2];
/**
 * C4-OB-02: destination for any residual ATA balance + the reclaimed ATA
 * rent. Bound to the escrow `client` (= the signer, via the escrow's
 * `has_one = client`) so the sweep cannot be redirected.
 */
clientTokenAccount: TAccountMetas[3];
tokenProgram: TAccountMetas[4];
};
data: CloseEscrowInstructionData; };

export function parseCloseEscrowInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedCloseEscrowInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { client: getNextAccount(), escrow: getNextAccount(), escrowTokenAccount: getNextAccount(), clientTokenAccount: getNextAccount(), tokenProgram: getNextAccount() }, data: getCloseEscrowInstructionDataDecoder().decode(instruction.data) };
}