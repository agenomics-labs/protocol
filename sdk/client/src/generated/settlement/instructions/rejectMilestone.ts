/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { SETTLEMENT_PROGRAM_ADDRESS } from '../programs/index.js';

export const REJECT_MILESTONE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([243, 48, 66, 165, 237, 41, 116, 249]);

export function getRejectMilestoneDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(REJECT_MILESTONE_DISCRIMINATOR); }

export type RejectMilestoneInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountClient extends string | AccountMeta<string> = string, TAccountEscrow extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountClient extends string ? WritableSignerAccount<TAccountClient> & AccountSignerMeta<TAccountClient> : TAccountClient, TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, ...TRemainingAccounts]>;

export type RejectMilestoneInstructionData = { discriminator: ReadonlyUint8Array; milestoneIndex: number;  };

export type RejectMilestoneInstructionDataArgs = { milestoneIndex: number;  };

export function getRejectMilestoneInstructionDataEncoder(): FixedSizeEncoder<RejectMilestoneInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['milestoneIndex', getU32Encoder()]]), (value) => ({ ...value, discriminator: REJECT_MILESTONE_DISCRIMINATOR }));
}

export function getRejectMilestoneInstructionDataDecoder(): FixedSizeDecoder<RejectMilestoneInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['milestoneIndex', getU32Decoder()]]);
}

export function getRejectMilestoneInstructionDataCodec(): FixedSizeCodec<RejectMilestoneInstructionDataArgs, RejectMilestoneInstructionData> {
    return combineCodec(getRejectMilestoneInstructionDataEncoder(), getRejectMilestoneInstructionDataDecoder());
}

export type RejectMilestoneInput<TAccountClient extends string = string, TAccountEscrow extends string = string> =  {
  client: TransactionSigner<TAccountClient>;
/**
 * AUD-019: Defense-in-depth — hoist the `escrow.status == Active`
 * check from the handler `require!` into an Anchor account-level
 * constraint. Handler `require!` is preserved as defense-in-depth.
 */
escrow: Address<TAccountEscrow>;
milestoneIndex: RejectMilestoneInstructionDataArgs["milestoneIndex"];
}

export function getRejectMilestoneInstruction<TAccountClient extends string, TAccountEscrow extends string, TProgramAddress extends Address = typeof SETTLEMENT_PROGRAM_ADDRESS>(input: RejectMilestoneInput<TAccountClient, TAccountEscrow>, config?: { programAddress?: TProgramAddress } ): RejectMilestoneInstruction<TProgramAddress, TAccountClient, TAccountEscrow> {
  // Program address.
const programAddress = config?.programAddress ?? SETTLEMENT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { client: { value: input.client ?? null, isWritable: true }, escrow: { value: input.escrow ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("client", accounts.client), getAccountMeta("escrow", accounts.escrow)], data: getRejectMilestoneInstructionDataEncoder().encode(args as RejectMilestoneInstructionDataArgs), programAddress } as RejectMilestoneInstruction<TProgramAddress, TAccountClient, TAccountEscrow>);
}

export type ParsedRejectMilestoneInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
client: TAccountMetas[0];
/**
 * AUD-019: Defense-in-depth — hoist the `escrow.status == Active`
 * check from the handler `require!` into an Anchor account-level
 * constraint. Handler `require!` is preserved as defense-in-depth.
 */
escrow: TAccountMetas[1];
};
data: RejectMilestoneInstructionData; };

export function parseRejectMilestoneInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedRejectMilestoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { client: getNextAccount(), escrow: getNextAccount() }, data: getRejectMilestoneInstructionDataDecoder().decode(instruction.data) };
}