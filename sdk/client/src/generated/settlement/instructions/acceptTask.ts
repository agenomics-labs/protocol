/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { SETTLEMENT_PROGRAM_ADDRESS } from '../programs/index.js';

export const ACCEPT_TASK_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([222, 196, 79, 165, 120, 30, 38, 120]);

export function getAcceptTaskDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(ACCEPT_TASK_DISCRIMINATOR); }

export type AcceptTaskInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountProvider extends string | AccountMeta<string> = string, TAccountEscrow extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountProvider extends string ? WritableSignerAccount<TAccountProvider> & AccountSignerMeta<TAccountProvider> : TAccountProvider, TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, ...TRemainingAccounts]>;

export type AcceptTaskInstructionData = { discriminator: ReadonlyUint8Array;  };

export type AcceptTaskInstructionDataArgs = {  };

export function getAcceptTaskInstructionDataEncoder(): FixedSizeEncoder<AcceptTaskInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: ACCEPT_TASK_DISCRIMINATOR }));
}

export function getAcceptTaskInstructionDataDecoder(): FixedSizeDecoder<AcceptTaskInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getAcceptTaskInstructionDataCodec(): FixedSizeCodec<AcceptTaskInstructionDataArgs, AcceptTaskInstructionData> {
    return combineCodec(getAcceptTaskInstructionDataEncoder(), getAcceptTaskInstructionDataDecoder());
}

export type AcceptTaskInput<TAccountProvider extends string = string, TAccountEscrow extends string = string> =  {
  provider: TransactionSigner<TAccountProvider>;
/**
 * AUD-019: Defense-in-depth — hoist the `escrow.status == Created`
 * check from the handler `require!` into an Anchor account-level
 * constraint so the Account-deserialization layer (not the handler)
 * is the first line of defense. The handler still re-checks via
 * `require!` for belt-and-suspenders parity with `CloseEscrow`.
 */
escrow: Address<TAccountEscrow>;
}

export function getAcceptTaskInstruction<TAccountProvider extends string, TAccountEscrow extends string, TProgramAddress extends Address = typeof SETTLEMENT_PROGRAM_ADDRESS>(input: AcceptTaskInput<TAccountProvider, TAccountEscrow>, config?: { programAddress?: TProgramAddress } ): AcceptTaskInstruction<TProgramAddress, TAccountProvider, TAccountEscrow> {
  // Program address.
const programAddress = config?.programAddress ?? SETTLEMENT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { provider: { value: input.provider ?? null, isWritable: true }, escrow: { value: input.escrow ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("provider", accounts.provider), getAccountMeta("escrow", accounts.escrow)], data: getAcceptTaskInstructionDataEncoder().encode({}), programAddress } as AcceptTaskInstruction<TProgramAddress, TAccountProvider, TAccountEscrow>);
}

export type ParsedAcceptTaskInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
provider: TAccountMetas[0];
/**
 * AUD-019: Defense-in-depth — hoist the `escrow.status == Created`
 * check from the handler `require!` into an Anchor account-level
 * constraint so the Account-deserialization layer (not the handler)
 * is the first line of defense. The handler still re-checks via
 * `require!` for belt-and-suspenders parity with `CloseEscrow`.
 */
escrow: TAccountMetas[1];
};
data: AcceptTaskInstructionData; };

export function parseAcceptTaskInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedAcceptTaskInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { provider: getNextAccount(), escrow: getNextAccount() }, data: getAcceptTaskInstructionDataDecoder().decode(instruction.data) };
}