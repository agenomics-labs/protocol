/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { SETTLEMENT_PROGRAM_ADDRESS } from '../programs/index.js';

export const SUBMIT_MILESTONE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([35, 96, 220, 215, 102, 83, 139, 52]);

export function getSubmitMilestoneDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(SUBMIT_MILESTONE_DISCRIMINATOR); }

export type SubmitMilestoneInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountProvider extends string | AccountMeta<string> = string, TAccountEscrow extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountProvider extends string ? WritableSignerAccount<TAccountProvider> & AccountSignerMeta<TAccountProvider> : TAccountProvider, TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, ...TRemainingAccounts]>;

export type SubmitMilestoneInstructionData = { discriminator: ReadonlyUint8Array; milestoneIndex: number; gracePeriodSlots: bigint;  };

export type SubmitMilestoneInstructionDataArgs = { milestoneIndex: number; gracePeriodSlots: number | bigint;  };

export function getSubmitMilestoneInstructionDataEncoder(): FixedSizeEncoder<SubmitMilestoneInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['milestoneIndex', getU32Encoder()], ['gracePeriodSlots', getU64Encoder()]]), (value) => ({ ...value, discriminator: SUBMIT_MILESTONE_DISCRIMINATOR }));
}

export function getSubmitMilestoneInstructionDataDecoder(): FixedSizeDecoder<SubmitMilestoneInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['milestoneIndex', getU32Decoder()], ['gracePeriodSlots', getU64Decoder()]]);
}

export function getSubmitMilestoneInstructionDataCodec(): FixedSizeCodec<SubmitMilestoneInstructionDataArgs, SubmitMilestoneInstructionData> {
    return combineCodec(getSubmitMilestoneInstructionDataEncoder(), getSubmitMilestoneInstructionDataDecoder());
}

export type SubmitMilestoneInput<TAccountProvider extends string = string, TAccountEscrow extends string = string> =  {
  provider: TransactionSigner<TAccountProvider>;
/**
 * AUD-019: Defense-in-depth — hoist the `escrow.status == Active`
 * check from the handler `require!` into an Anchor account-level
 * constraint. Handler `require!` is preserved as defense-in-depth.
 */
escrow: Address<TAccountEscrow>;
milestoneIndex: SubmitMilestoneInstructionDataArgs["milestoneIndex"];
gracePeriodSlots: SubmitMilestoneInstructionDataArgs["gracePeriodSlots"];
}

export function getSubmitMilestoneInstruction<TAccountProvider extends string, TAccountEscrow extends string, TProgramAddress extends Address = typeof SETTLEMENT_PROGRAM_ADDRESS>(input: SubmitMilestoneInput<TAccountProvider, TAccountEscrow>, config?: { programAddress?: TProgramAddress } ): SubmitMilestoneInstruction<TProgramAddress, TAccountProvider, TAccountEscrow> {
  // Program address.
const programAddress = config?.programAddress ?? SETTLEMENT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { provider: { value: input.provider ?? null, isWritable: true }, escrow: { value: input.escrow ?? null, isWritable: true } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("provider", accounts.provider), getAccountMeta("escrow", accounts.escrow)], data: getSubmitMilestoneInstructionDataEncoder().encode(args as SubmitMilestoneInstructionDataArgs), programAddress } as SubmitMilestoneInstruction<TProgramAddress, TAccountProvider, TAccountEscrow>);
}

export type ParsedSubmitMilestoneInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
provider: TAccountMetas[0];
/**
 * AUD-019: Defense-in-depth — hoist the `escrow.status == Active`
 * check from the handler `require!` into an Anchor account-level
 * constraint. Handler `require!` is preserved as defense-in-depth.
 */
escrow: TAccountMetas[1];
};
data: SubmitMilestoneInstructionData; };

export function parseSubmitMilestoneInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedSubmitMilestoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 2 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { provider: getNextAccount(), escrow: getNextAccount() }, data: getSubmitMilestoneInstructionDataDecoder().decode(instruction.data) };
}