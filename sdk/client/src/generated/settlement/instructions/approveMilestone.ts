/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU8Decoder, getU8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findProtocolConfigPda, findSettlementAuthorityPda } from '../pdas/index.js';
import { SETTLEMENT_PROGRAM_ADDRESS } from '../programs/index.js';

export const APPROVE_MILESTONE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([145, 85, 92, 60, 50, 130, 219, 106]);

export function getApproveMilestoneDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(APPROVE_MILESTONE_DISCRIMINATOR); }

export type ApproveMilestoneInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountClient extends string | AccountMeta<string> = string, TAccountEscrow extends string | AccountMeta<string> = string, TAccountEscrowTokenAccount extends string | AccountMeta<string> = string, TAccountProviderTokenAccount extends string | AccountMeta<string> = string, TAccountRegistryProgram extends string | AccountMeta<string> = string, TAccountProviderAuthority extends string | AccountMeta<string> = string, TAccountProviderOwnerNonce extends string | AccountMeta<string> = string, TAccountProviderProfile extends string | AccountMeta<string> = string, TAccountSettlementAuthority extends string | AccountMeta<string> = string, TAccountProtocolConfig extends string | AccountMeta<string> = string, TAccountTokenProgram extends string | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountClient extends string ? WritableSignerAccount<TAccountClient> & AccountSignerMeta<TAccountClient> : TAccountClient, TAccountEscrow extends string ? WritableAccount<TAccountEscrow> : TAccountEscrow, TAccountEscrowTokenAccount extends string ? WritableAccount<TAccountEscrowTokenAccount> : TAccountEscrowTokenAccount, TAccountProviderTokenAccount extends string ? WritableAccount<TAccountProviderTokenAccount> : TAccountProviderTokenAccount, TAccountRegistryProgram extends string ? ReadonlyAccount<TAccountRegistryProgram> : TAccountRegistryProgram, TAccountProviderAuthority extends string ? ReadonlyAccount<TAccountProviderAuthority> : TAccountProviderAuthority, TAccountProviderOwnerNonce extends string ? ReadonlyAccount<TAccountProviderOwnerNonce> : TAccountProviderOwnerNonce, TAccountProviderProfile extends string ? WritableAccount<TAccountProviderProfile> : TAccountProviderProfile, TAccountSettlementAuthority extends string ? ReadonlyAccount<TAccountSettlementAuthority> : TAccountSettlementAuthority, TAccountProtocolConfig extends string ? ReadonlyAccount<TAccountProtocolConfig> : TAccountProtocolConfig, TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram, ...TRemainingAccounts]>;

export type ApproveMilestoneInstructionData = { discriminator: ReadonlyUint8Array; milestoneIndex: number; rating: number;  };

export type ApproveMilestoneInstructionDataArgs = { milestoneIndex: number; rating: number;  };

export function getApproveMilestoneInstructionDataEncoder(): FixedSizeEncoder<ApproveMilestoneInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['milestoneIndex', getU32Encoder()], ['rating', getU8Encoder()]]), (value) => ({ ...value, discriminator: APPROVE_MILESTONE_DISCRIMINATOR }));
}

export function getApproveMilestoneInstructionDataDecoder(): FixedSizeDecoder<ApproveMilestoneInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['milestoneIndex', getU32Decoder()], ['rating', getU8Decoder()]]);
}

export function getApproveMilestoneInstructionDataCodec(): FixedSizeCodec<ApproveMilestoneInstructionDataArgs, ApproveMilestoneInstructionData> {
    return combineCodec(getApproveMilestoneInstructionDataEncoder(), getApproveMilestoneInstructionDataDecoder());
}

export type ApproveMilestoneAsyncInput<TAccountClient extends string = string, TAccountEscrow extends string = string, TAccountEscrowTokenAccount extends string = string, TAccountProviderTokenAccount extends string = string, TAccountRegistryProgram extends string = string, TAccountProviderAuthority extends string = string, TAccountProviderOwnerNonce extends string = string, TAccountProviderProfile extends string = string, TAccountSettlementAuthority extends string = string, TAccountProtocolConfig extends string = string, TAccountTokenProgram extends string = string> =  {
  client: TransactionSigner<TAccountClient>;
escrow: Address<TAccountEscrow>;
escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
providerTokenAccount: Address<TAccountProviderTokenAccount>;
/** Agent Registry program for CPI reputation updates. */
registryProgram: Address<TAccountRegistryProgram>;
/**
 * SEC-1 (per ADR-068, Accepted 2026-04-23): external authority anchor for the
 * Registry's `ProposeReputationDelta` CPI. The Registry now pins
 * `agent_profile` via `has_one = authority` + seeds derived from
 * `authority.key()` instead of the self-referential
 * `agent_profile.authority`. Feeding `escrow.provider` here is the
 * correct match: `provider_profile`'s seeds (declared after this
 * field, deliberately) are also pinned to `escrow.provider`.
 *
 * AUD-117 (cycle-2): hoisted ahead of `provider_owner_nonce` and
 * `provider_profile` so those fields can reference its key in their
 * seeds constraints. Anchor processes fields in declaration order.
 */
providerAuthority: Address<TAccountProviderAuthority>;
/**
 * AUD-117 (cycle-2): defense-in-depth seeds constraint. The Registry's
 * `ProposeReputationDelta` context already validates this PDA, but
 * re-deriving here at the Settlement boundary protects against any
 * future Registry refactor that loosens its seeds discipline. Typed
 * as `Account<'info, OwnerNonce>` so the `.nonce` field is accessible
 * to the `provider_profile` seeds below.
 */
providerOwnerNonce?: Address<TAccountProviderOwnerNonce>;
/**
 * Provider's AgentProfile PDA — re-derived at the Settlement boundary
 * using `[provider_authority, b"agent-profile", provider_owner_nonce.nonce]`
 * (mirrors the Registry's `ProposeReputationDelta` seeds with
 * `seeds::program = AGENT_REGISTRY_PROGRAM_ID`).
 *
 * AUD-117 (cycle-2): defense-in-depth — the same PDA derivation that
 * the Registry CPI's seeds constraint will perform on the callee side.
 */
providerProfile: Address<TAccountProviderProfile>;
/**
 * Settlement authority PDA — this program's signing authority for CPI calls.
 * The Registry program verifies this PDA as a signer with seeds::program = SETTLEMENT_PROGRAM_ID.
 * SEC-8 (per ADR-074, Accepted 2026-04-23): `seeds::program` is already the
 * program's own ID by default, but making it explicit blocks any
 * future refactor from silently changing the derivation surface.
 */
settlementAuthority?: Address<TAccountSettlementAuthority>;
/** Finding #19: Reads the governance-owned `reputation_delta_task_completed`. */
protocolConfig?: Address<TAccountProtocolConfig>;
tokenProgram?: Address<TAccountTokenProgram>;
milestoneIndex: ApproveMilestoneInstructionDataArgs["milestoneIndex"];
rating: ApproveMilestoneInstructionDataArgs["rating"];
}

export async function getApproveMilestoneInstructionAsync<TAccountClient extends string, TAccountEscrow extends string, TAccountEscrowTokenAccount extends string, TAccountProviderTokenAccount extends string, TAccountRegistryProgram extends string, TAccountProviderAuthority extends string, TAccountProviderOwnerNonce extends string, TAccountProviderProfile extends string, TAccountSettlementAuthority extends string, TAccountProtocolConfig extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof SETTLEMENT_PROGRAM_ADDRESS>(input: ApproveMilestoneAsyncInput<TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountProviderTokenAccount, TAccountRegistryProgram, TAccountProviderAuthority, TAccountProviderOwnerNonce, TAccountProviderProfile, TAccountSettlementAuthority, TAccountProtocolConfig, TAccountTokenProgram>, config?: { programAddress?: TProgramAddress } ): Promise<ApproveMilestoneInstruction<TProgramAddress, TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountProviderTokenAccount, TAccountRegistryProgram, TAccountProviderAuthority, TAccountProviderOwnerNonce, TAccountProviderProfile, TAccountSettlementAuthority, TAccountProtocolConfig, TAccountTokenProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? SETTLEMENT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { client: { value: input.client ?? null, isWritable: true }, escrow: { value: input.escrow ?? null, isWritable: true }, escrowTokenAccount: { value: input.escrowTokenAccount ?? null, isWritable: true }, providerTokenAccount: { value: input.providerTokenAccount ?? null, isWritable: true }, registryProgram: { value: input.registryProgram ?? null, isWritable: false }, providerAuthority: { value: input.providerAuthority ?? null, isWritable: false }, providerOwnerNonce: { value: input.providerOwnerNonce ?? null, isWritable: false }, providerProfile: { value: input.providerProfile ?? null, isWritable: true }, settlementAuthority: { value: input.settlementAuthority ?? null, isWritable: false }, protocolConfig: { value: input.protocolConfig ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.providerOwnerNonce.value) {
accounts.providerOwnerNonce.value = await getProgramDerivedAddress({ programAddress: 'psJT29X5QAqkc9ZL3mt1YbyUsGqgdXjBU7RhEUEyNyv' as Address<'psJT29X5QAqkc9ZL3mt1YbyUsGqgdXjBU7RhEUEyNyv'>, seeds: [getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("providerAuthority", accounts.providerAuthority.value)), getBytesEncoder().encode(new Uint8Array([111, 119, 110, 101, 114, 45, 110, 111, 110, 99, 101]))] });
}
if (!accounts.settlementAuthority.value) {
accounts.settlementAuthority.value = await findSettlementAuthorityPda();
}
if (!accounts.protocolConfig.value) {
accounts.protocolConfig.value = await findProtocolConfigPda();
}
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("client", accounts.client), getAccountMeta("escrow", accounts.escrow), getAccountMeta("escrowTokenAccount", accounts.escrowTokenAccount), getAccountMeta("providerTokenAccount", accounts.providerTokenAccount), getAccountMeta("registryProgram", accounts.registryProgram), getAccountMeta("providerAuthority", accounts.providerAuthority), getAccountMeta("providerOwnerNonce", accounts.providerOwnerNonce), getAccountMeta("providerProfile", accounts.providerProfile), getAccountMeta("settlementAuthority", accounts.settlementAuthority), getAccountMeta("protocolConfig", accounts.protocolConfig), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getApproveMilestoneInstructionDataEncoder().encode(args as ApproveMilestoneInstructionDataArgs), programAddress } as ApproveMilestoneInstruction<TProgramAddress, TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountProviderTokenAccount, TAccountRegistryProgram, TAccountProviderAuthority, TAccountProviderOwnerNonce, TAccountProviderProfile, TAccountSettlementAuthority, TAccountProtocolConfig, TAccountTokenProgram>);
}

export type ApproveMilestoneInput<TAccountClient extends string = string, TAccountEscrow extends string = string, TAccountEscrowTokenAccount extends string = string, TAccountProviderTokenAccount extends string = string, TAccountRegistryProgram extends string = string, TAccountProviderAuthority extends string = string, TAccountProviderOwnerNonce extends string = string, TAccountProviderProfile extends string = string, TAccountSettlementAuthority extends string = string, TAccountProtocolConfig extends string = string, TAccountTokenProgram extends string = string> =  {
  client: TransactionSigner<TAccountClient>;
escrow: Address<TAccountEscrow>;
escrowTokenAccount: Address<TAccountEscrowTokenAccount>;
providerTokenAccount: Address<TAccountProviderTokenAccount>;
/** Agent Registry program for CPI reputation updates. */
registryProgram: Address<TAccountRegistryProgram>;
/**
 * SEC-1 (per ADR-068, Accepted 2026-04-23): external authority anchor for the
 * Registry's `ProposeReputationDelta` CPI. The Registry now pins
 * `agent_profile` via `has_one = authority` + seeds derived from
 * `authority.key()` instead of the self-referential
 * `agent_profile.authority`. Feeding `escrow.provider` here is the
 * correct match: `provider_profile`'s seeds (declared after this
 * field, deliberately) are also pinned to `escrow.provider`.
 *
 * AUD-117 (cycle-2): hoisted ahead of `provider_owner_nonce` and
 * `provider_profile` so those fields can reference its key in their
 * seeds constraints. Anchor processes fields in declaration order.
 */
providerAuthority: Address<TAccountProviderAuthority>;
/**
 * AUD-117 (cycle-2): defense-in-depth seeds constraint. The Registry's
 * `ProposeReputationDelta` context already validates this PDA, but
 * re-deriving here at the Settlement boundary protects against any
 * future Registry refactor that loosens its seeds discipline. Typed
 * as `Account<'info, OwnerNonce>` so the `.nonce` field is accessible
 * to the `provider_profile` seeds below.
 */
providerOwnerNonce: Address<TAccountProviderOwnerNonce>;
/**
 * Provider's AgentProfile PDA — re-derived at the Settlement boundary
 * using `[provider_authority, b"agent-profile", provider_owner_nonce.nonce]`
 * (mirrors the Registry's `ProposeReputationDelta` seeds with
 * `seeds::program = AGENT_REGISTRY_PROGRAM_ID`).
 *
 * AUD-117 (cycle-2): defense-in-depth — the same PDA derivation that
 * the Registry CPI's seeds constraint will perform on the callee side.
 */
providerProfile: Address<TAccountProviderProfile>;
/**
 * Settlement authority PDA — this program's signing authority for CPI calls.
 * The Registry program verifies this PDA as a signer with seeds::program = SETTLEMENT_PROGRAM_ID.
 * SEC-8 (per ADR-074, Accepted 2026-04-23): `seeds::program` is already the
 * program's own ID by default, but making it explicit blocks any
 * future refactor from silently changing the derivation surface.
 */
settlementAuthority: Address<TAccountSettlementAuthority>;
/** Finding #19: Reads the governance-owned `reputation_delta_task_completed`. */
protocolConfig: Address<TAccountProtocolConfig>;
tokenProgram?: Address<TAccountTokenProgram>;
milestoneIndex: ApproveMilestoneInstructionDataArgs["milestoneIndex"];
rating: ApproveMilestoneInstructionDataArgs["rating"];
}

export function getApproveMilestoneInstruction<TAccountClient extends string, TAccountEscrow extends string, TAccountEscrowTokenAccount extends string, TAccountProviderTokenAccount extends string, TAccountRegistryProgram extends string, TAccountProviderAuthority extends string, TAccountProviderOwnerNonce extends string, TAccountProviderProfile extends string, TAccountSettlementAuthority extends string, TAccountProtocolConfig extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof SETTLEMENT_PROGRAM_ADDRESS>(input: ApproveMilestoneInput<TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountProviderTokenAccount, TAccountRegistryProgram, TAccountProviderAuthority, TAccountProviderOwnerNonce, TAccountProviderProfile, TAccountSettlementAuthority, TAccountProtocolConfig, TAccountTokenProgram>, config?: { programAddress?: TProgramAddress } ): ApproveMilestoneInstruction<TProgramAddress, TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountProviderTokenAccount, TAccountRegistryProgram, TAccountProviderAuthority, TAccountProviderOwnerNonce, TAccountProviderProfile, TAccountSettlementAuthority, TAccountProtocolConfig, TAccountTokenProgram> {
  // Program address.
const programAddress = config?.programAddress ?? SETTLEMENT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { client: { value: input.client ?? null, isWritable: true }, escrow: { value: input.escrow ?? null, isWritable: true }, escrowTokenAccount: { value: input.escrowTokenAccount ?? null, isWritable: true }, providerTokenAccount: { value: input.providerTokenAccount ?? null, isWritable: true }, registryProgram: { value: input.registryProgram ?? null, isWritable: false }, providerAuthority: { value: input.providerAuthority ?? null, isWritable: false }, providerOwnerNonce: { value: input.providerOwnerNonce ?? null, isWritable: false }, providerProfile: { value: input.providerProfile ?? null, isWritable: true }, settlementAuthority: { value: input.settlementAuthority ?? null, isWritable: false }, protocolConfig: { value: input.protocolConfig ?? null, isWritable: false }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("client", accounts.client), getAccountMeta("escrow", accounts.escrow), getAccountMeta("escrowTokenAccount", accounts.escrowTokenAccount), getAccountMeta("providerTokenAccount", accounts.providerTokenAccount), getAccountMeta("registryProgram", accounts.registryProgram), getAccountMeta("providerAuthority", accounts.providerAuthority), getAccountMeta("providerOwnerNonce", accounts.providerOwnerNonce), getAccountMeta("providerProfile", accounts.providerProfile), getAccountMeta("settlementAuthority", accounts.settlementAuthority), getAccountMeta("protocolConfig", accounts.protocolConfig), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getApproveMilestoneInstructionDataEncoder().encode(args as ApproveMilestoneInstructionDataArgs), programAddress } as ApproveMilestoneInstruction<TProgramAddress, TAccountClient, TAccountEscrow, TAccountEscrowTokenAccount, TAccountProviderTokenAccount, TAccountRegistryProgram, TAccountProviderAuthority, TAccountProviderOwnerNonce, TAccountProviderProfile, TAccountSettlementAuthority, TAccountProtocolConfig, TAccountTokenProgram>);
}

export type ParsedApproveMilestoneInstruction<TProgram extends string = typeof SETTLEMENT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
client: TAccountMetas[0];
escrow: TAccountMetas[1];
escrowTokenAccount: TAccountMetas[2];
providerTokenAccount: TAccountMetas[3];
/** Agent Registry program for CPI reputation updates. */
registryProgram: TAccountMetas[4];
/**
 * SEC-1 (per ADR-068, Accepted 2026-04-23): external authority anchor for the
 * Registry's `ProposeReputationDelta` CPI. The Registry now pins
 * `agent_profile` via `has_one = authority` + seeds derived from
 * `authority.key()` instead of the self-referential
 * `agent_profile.authority`. Feeding `escrow.provider` here is the
 * correct match: `provider_profile`'s seeds (declared after this
 * field, deliberately) are also pinned to `escrow.provider`.
 *
 * AUD-117 (cycle-2): hoisted ahead of `provider_owner_nonce` and
 * `provider_profile` so those fields can reference its key in their
 * seeds constraints. Anchor processes fields in declaration order.
 */
providerAuthority: TAccountMetas[5];
/**
 * AUD-117 (cycle-2): defense-in-depth seeds constraint. The Registry's
 * `ProposeReputationDelta` context already validates this PDA, but
 * re-deriving here at the Settlement boundary protects against any
 * future Registry refactor that loosens its seeds discipline. Typed
 * as `Account<'info, OwnerNonce>` so the `.nonce` field is accessible
 * to the `provider_profile` seeds below.
 */
providerOwnerNonce: TAccountMetas[6];
/**
 * Provider's AgentProfile PDA — re-derived at the Settlement boundary
 * using `[provider_authority, b"agent-profile", provider_owner_nonce.nonce]`
 * (mirrors the Registry's `ProposeReputationDelta` seeds with
 * `seeds::program = AGENT_REGISTRY_PROGRAM_ID`).
 *
 * AUD-117 (cycle-2): defense-in-depth — the same PDA derivation that
 * the Registry CPI's seeds constraint will perform on the callee side.
 */
providerProfile: TAccountMetas[7];
/**
 * Settlement authority PDA — this program's signing authority for CPI calls.
 * The Registry program verifies this PDA as a signer with seeds::program = SETTLEMENT_PROGRAM_ID.
 * SEC-8 (per ADR-074, Accepted 2026-04-23): `seeds::program` is already the
 * program's own ID by default, but making it explicit blocks any
 * future refactor from silently changing the derivation surface.
 */
settlementAuthority: TAccountMetas[8];
/** Finding #19: Reads the governance-owned `reputation_delta_task_completed`. */
protocolConfig: TAccountMetas[9];
tokenProgram: TAccountMetas[10];
};
data: ApproveMilestoneInstructionData; };

export function parseApproveMilestoneInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedApproveMilestoneInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 11) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 11 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { client: getNextAccount(), escrow: getNextAccount(), escrowTokenAccount: getNextAccount(), providerTokenAccount: getNextAccount(), registryProgram: getNextAccount(), providerAuthority: getNextAccount(), providerOwnerNonce: getNextAccount(), providerProfile: getNextAccount(), settlementAuthority: getNextAccount(), protocolConfig: getNextAccount(), tokenProgram: getNextAccount() }, data: getApproveMilestoneInstructionDataDecoder().decode(instruction.data) };
}