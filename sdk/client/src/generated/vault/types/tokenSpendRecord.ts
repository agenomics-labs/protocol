/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getAddressDecoder, getAddressEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder } from '@solana/kit';

/**
 * Tracks per-token daily spending for a specific mint.
 *
 * Findings #13/#14: Each record now carries its own `per_tx_limit` and
 * `daily_limit` expressed in the token's base units. This replaces the
 * previous scheme where the vault's SOL-lamport `daily_limit_lamports`
 * was reused as the cap for every mint, conflating decimal schemes
 * (0.01 SOL = 10M lamports vs. 10M USDC base units = 10 USDC).
 */
export type TokenSpendRecord = { 
/** The SPL token mint this record tracks. */
mint: Address; 
/** Maximum amount of this token transferable in a single tx (base units). */
perTxLimit: bigint; 
/** Maximum amount of this token transferable per day (base units). */
dailyLimit: bigint; 
/** Amount of this token spent today (in base units). */
spentToday: bigint; 
/** The day for which spent_today is tracked (Unix timestamp / 86400). */
lastSpendDay: bigint;  };

export type TokenSpendRecordArgs = { 
/** The SPL token mint this record tracks. */
mint: Address; 
/** Maximum amount of this token transferable in a single tx (base units). */
perTxLimit: number | bigint; 
/** Maximum amount of this token transferable per day (base units). */
dailyLimit: number | bigint; 
/** Amount of this token spent today (in base units). */
spentToday: number | bigint; 
/** The day for which spent_today is tracked (Unix timestamp / 86400). */
lastSpendDay: number | bigint;  };

export function getTokenSpendRecordEncoder(): FixedSizeEncoder<TokenSpendRecordArgs> {
    return getStructEncoder([['mint', getAddressEncoder()], ['perTxLimit', getU64Encoder()], ['dailyLimit', getU64Encoder()], ['spentToday', getU64Encoder()], ['lastSpendDay', getU64Encoder()]]);
}

export function getTokenSpendRecordDecoder(): FixedSizeDecoder<TokenSpendRecord> {
    return getStructDecoder([['mint', getAddressDecoder()], ['perTxLimit', getU64Decoder()], ['dailyLimit', getU64Decoder()], ['spentToday', getU64Decoder()], ['lastSpendDay', getU64Decoder()]]);
}

export function getTokenSpendRecordCodec(): FixedSizeCodec<TokenSpendRecordArgs, TokenSpendRecord> {
    return combineCodec(getTokenSpendRecordEncoder(), getTokenSpendRecordDecoder());
}