/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getAddressDecoder, getAddressEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder } from '@solana/kit';

/**
 * ADR-111: Per-mint spend cap for a delegation grant.
 *
 * Mirrors the shape of `TokenSpendRecord` but is grant-local — the
 * grantee's spending under a grant is capped per-mint INDEPENDENTLY of
 * the parent vault's daily allocation. A transfer must satisfy BOTH the
 * grant cap AND the vault's per-mint daily cap (see
 * `execute_grant_token_transfer`).
 */
export type GrantTokenCap = { 
/** The SPL token mint this cap covers. */
mint: Address; 
/** Lifetime cumulative cap for this mint, in base units. */
cap: bigint; 
/** Lifetime spent under this grant for this mint, in base units. */
spent: bigint;  };

export type GrantTokenCapArgs = { 
/** The SPL token mint this cap covers. */
mint: Address; 
/** Lifetime cumulative cap for this mint, in base units. */
cap: number | bigint; 
/** Lifetime spent under this grant for this mint, in base units. */
spent: number | bigint;  };

export function getGrantTokenCapEncoder(): FixedSizeEncoder<GrantTokenCapArgs> {
    return getStructEncoder([['mint', getAddressEncoder()], ['cap', getU64Encoder()], ['spent', getU64Encoder()]]);
}

export function getGrantTokenCapDecoder(): FixedSizeDecoder<GrantTokenCap> {
    return getStructDecoder([['mint', getAddressDecoder()], ['cap', getU64Decoder()], ['spent', getU64Decoder()]]);
}

export function getGrantTokenCapCodec(): FixedSizeCodec<GrantTokenCapArgs, GrantTokenCap> {
    return combineCodec(getGrantTokenCapEncoder(), getGrantTokenCapDecoder());
}