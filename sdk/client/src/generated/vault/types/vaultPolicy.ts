/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, type Address, type Codec, type Decoder, type Encoder } from '@solana/kit';

/** The spending policy for a vault. */
export type VaultPolicy = { 
/** Maximum SOL that can be transferred in a single transaction (in lamports). */
perTxLimitLamports: bigint; 
/** Maximum SOL that can be transferred per day (in lamports). */
dailyLimitLamports: bigint; 
/** Maximum number of transactions allowed per hour. */
maxTxsPerHour: number; 
/**
 * Bitmap for token allowlist (if None, all tokens allowed; if Some, only listed tokens).
 * This is a simplified version; production would use a separate account for large lists.
 */
tokenAllowlist: Array<Address>; 
/** Bitmap for program allowlist (if None, all programs allowed; if Some, only listed programs). */
programAllowlist: Array<Address>;  };

export type VaultPolicyArgs = { 
/** Maximum SOL that can be transferred in a single transaction (in lamports). */
perTxLimitLamports: number | bigint; 
/** Maximum SOL that can be transferred per day (in lamports). */
dailyLimitLamports: number | bigint; 
/** Maximum number of transactions allowed per hour. */
maxTxsPerHour: number; 
/**
 * Bitmap for token allowlist (if None, all tokens allowed; if Some, only listed tokens).
 * This is a simplified version; production would use a separate account for large lists.
 */
tokenAllowlist: Array<Address>; 
/** Bitmap for program allowlist (if None, all programs allowed; if Some, only listed programs). */
programAllowlist: Array<Address>;  };

export function getVaultPolicyEncoder(): Encoder<VaultPolicyArgs> {
    return getStructEncoder([['perTxLimitLamports', getU64Encoder()], ['dailyLimitLamports', getU64Encoder()], ['maxTxsPerHour', getU32Encoder()], ['tokenAllowlist', getArrayEncoder(getAddressEncoder())], ['programAllowlist', getArrayEncoder(getAddressEncoder())]]);
}

export function getVaultPolicyDecoder(): Decoder<VaultPolicy> {
    return getStructDecoder([['perTxLimitLamports', getU64Decoder()], ['dailyLimitLamports', getU64Decoder()], ['maxTxsPerHour', getU32Decoder()], ['tokenAllowlist', getArrayDecoder(getAddressDecoder())], ['programAllowlist', getArrayDecoder(getAddressDecoder())]]);
}

export function getVaultPolicyCodec(): Codec<VaultPolicyArgs, VaultPolicy> {
    return combineCodec(getVaultPolicyEncoder(), getVaultPolicyDecoder());
}