/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findVaultPda } from '../pdas/index.js';
import { AGENT_VAULT_PROGRAM_ADDRESS } from '../programs/index.js';

export const EXECUTE_TOKEN_TRANSFER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([56, 178, 89, 210, 83, 156, 33, 36]);

export function getExecuteTokenTransferDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(EXECUTE_TOKEN_TRANSFER_DISCRIMINATOR); }

export type ExecuteTokenTransferInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountVault extends string | AccountMeta<string> = string, TAccountAgent extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountAgentProfile extends string | AccountMeta<string> = string, TAccountVaultTokenAccount extends string | AccountMeta<string> = string, TAccountRecipientTokenAccount extends string | AccountMeta<string> = string, TAccountTokenProgram extends string | AccountMeta<string> = "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault, TAccountAgent extends string ? ReadonlySignerAccount<TAccountAgent> & AccountSignerMeta<TAccountAgent> : TAccountAgent, TAccountAuthority extends string ? ReadonlyAccount<TAccountAuthority> : TAccountAuthority, TAccountAgentProfile extends string ? ReadonlyAccount<TAccountAgentProfile> : TAccountAgentProfile, TAccountVaultTokenAccount extends string ? WritableAccount<TAccountVaultTokenAccount> : TAccountVaultTokenAccount, TAccountRecipientTokenAccount extends string ? WritableAccount<TAccountRecipientTokenAccount> : TAccountRecipientTokenAccount, TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram, ...TRemainingAccounts]>;

export type ExecuteTokenTransferInstructionData = { discriminator: ReadonlyUint8Array; amount: bigint; toolIdHash: ReadonlyUint8Array;  };

export type ExecuteTokenTransferInstructionDataArgs = { amount: number | bigint; toolIdHash: ReadonlyUint8Array;  };

export function getExecuteTokenTransferInstructionDataEncoder(): FixedSizeEncoder<ExecuteTokenTransferInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['amount', getU64Encoder()], ['toolIdHash', fixEncoderSize(getBytesEncoder(), 32)]]), (value) => ({ ...value, discriminator: EXECUTE_TOKEN_TRANSFER_DISCRIMINATOR }));
}

export function getExecuteTokenTransferInstructionDataDecoder(): FixedSizeDecoder<ExecuteTokenTransferInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['amount', getU64Decoder()], ['toolIdHash', fixDecoderSize(getBytesDecoder(), 32)]]);
}

export function getExecuteTokenTransferInstructionDataCodec(): FixedSizeCodec<ExecuteTokenTransferInstructionDataArgs, ExecuteTokenTransferInstructionData> {
    return combineCodec(getExecuteTokenTransferInstructionDataEncoder(), getExecuteTokenTransferInstructionDataDecoder());
}

export type ExecuteTokenTransferAsyncInput<TAccountVault extends string = string, TAccountAgent extends string = string, TAccountAuthority extends string = string, TAccountAgentProfile extends string = string, TAccountVaultTokenAccount extends string = string, TAccountRecipientTokenAccount extends string = string, TAccountTokenProgram extends string = string> =  {
  /**
 * ADR-093: Seeds use the externally-known `authority` account key rather
 * than `vault.authority` read from stored account data. The `has_one`
 * constraint verifies that `authority` matches `vault.authority`.
 */
vault?: Address<TAccountVault>;
/** The signer must be the agent authority or the vault authority. */
agent: TransactionSigner<TAccountAgent>;
/**
 * The vault authority — used as a canonical PDA seed (ADR-093).
 * Not required to be a signer; the `agent` field carries the signature.
 */
authority: Address<TAccountAuthority>;
/**
 * ADR-095: The agent's Registry profile. Read-only cross-program account.
 * Suspension check is done in the instruction handler.
 *
 * `authority.key()` and `vault.profile_nonce`. The suspension check
 * is done explicitly in the instruction handler using `AgentStatus`.
 */
agentProfile: Address<TAccountAgentProfile>;
/** The vault's token account (source of SPL tokens). Must be owned by the vault PDA. */
vaultTokenAccount: Address<TAccountVaultTokenAccount>;
/**
 * The recipient's token account.
 *
 * Constraints:
 * - Mint must match `vault_token_account.mint` (already-mitigated per
 * DEEP-AUDIT-2026-04-22 Audit 1 finding SEC-6 — retained as-is).
 * - ADR-072 (SEC-6): The recipient account must NOT be the vault's own
 * token account (the exact same account) AND its SPL owner must not
 * be the vault PDA. This blocks a self-transfer loop that would
 * otherwise let a griefer (or a compromised `agent_identity`) burn
 * rate-limit slots and exhaust the hourly window during incident
 * response — every self-transfer is a no-op at the token-program
 * layer but still increments `txs_in_current_window`.
 *
 * Note: the recipient-mint match was already present pre-ADR-072; the
 * recipient-owner / self-account checks are the net-new SEC-6 fix.
 */
recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
tokenProgram?: Address<TAccountTokenProgram>;
amount: ExecuteTokenTransferInstructionDataArgs["amount"];
toolIdHash: ExecuteTokenTransferInstructionDataArgs["toolIdHash"];
}

export async function getExecuteTokenTransferInstructionAsync<TAccountVault extends string, TAccountAgent extends string, TAccountAuthority extends string, TAccountAgentProfile extends string, TAccountVaultTokenAccount extends string, TAccountRecipientTokenAccount extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ExecuteTokenTransferAsyncInput<TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountVaultTokenAccount, TAccountRecipientTokenAccount, TAccountTokenProgram>, config?: { programAddress?: TProgramAddress } ): Promise<ExecuteTokenTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountVaultTokenAccount, TAccountRecipientTokenAccount, TAccountTokenProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, agent: { value: input.agent ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: false }, vaultTokenAccount: { value: input.vaultTokenAccount ?? null, isWritable: true }, recipientTokenAccount: { value: input.recipientTokenAccount ?? null, isWritable: true }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.vault.value) {
accounts.vault.value = await findVaultPda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("agent", accounts.agent), getAccountMeta("authority", accounts.authority), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount), getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getExecuteTokenTransferInstructionDataEncoder().encode(args as ExecuteTokenTransferInstructionDataArgs), programAddress } as ExecuteTokenTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountVaultTokenAccount, TAccountRecipientTokenAccount, TAccountTokenProgram>);
}

export type ExecuteTokenTransferInput<TAccountVault extends string = string, TAccountAgent extends string = string, TAccountAuthority extends string = string, TAccountAgentProfile extends string = string, TAccountVaultTokenAccount extends string = string, TAccountRecipientTokenAccount extends string = string, TAccountTokenProgram extends string = string> =  {
  /**
 * ADR-093: Seeds use the externally-known `authority` account key rather
 * than `vault.authority` read from stored account data. The `has_one`
 * constraint verifies that `authority` matches `vault.authority`.
 */
vault: Address<TAccountVault>;
/** The signer must be the agent authority or the vault authority. */
agent: TransactionSigner<TAccountAgent>;
/**
 * The vault authority — used as a canonical PDA seed (ADR-093).
 * Not required to be a signer; the `agent` field carries the signature.
 */
authority: Address<TAccountAuthority>;
/**
 * ADR-095: The agent's Registry profile. Read-only cross-program account.
 * Suspension check is done in the instruction handler.
 *
 * `authority.key()` and `vault.profile_nonce`. The suspension check
 * is done explicitly in the instruction handler using `AgentStatus`.
 */
agentProfile: Address<TAccountAgentProfile>;
/** The vault's token account (source of SPL tokens). Must be owned by the vault PDA. */
vaultTokenAccount: Address<TAccountVaultTokenAccount>;
/**
 * The recipient's token account.
 *
 * Constraints:
 * - Mint must match `vault_token_account.mint` (already-mitigated per
 * DEEP-AUDIT-2026-04-22 Audit 1 finding SEC-6 — retained as-is).
 * - ADR-072 (SEC-6): The recipient account must NOT be the vault's own
 * token account (the exact same account) AND its SPL owner must not
 * be the vault PDA. This blocks a self-transfer loop that would
 * otherwise let a griefer (or a compromised `agent_identity`) burn
 * rate-limit slots and exhaust the hourly window during incident
 * response — every self-transfer is a no-op at the token-program
 * layer but still increments `txs_in_current_window`.
 *
 * Note: the recipient-mint match was already present pre-ADR-072; the
 * recipient-owner / self-account checks are the net-new SEC-6 fix.
 */
recipientTokenAccount: Address<TAccountRecipientTokenAccount>;
tokenProgram?: Address<TAccountTokenProgram>;
amount: ExecuteTokenTransferInstructionDataArgs["amount"];
toolIdHash: ExecuteTokenTransferInstructionDataArgs["toolIdHash"];
}

export function getExecuteTokenTransferInstruction<TAccountVault extends string, TAccountAgent extends string, TAccountAuthority extends string, TAccountAgentProfile extends string, TAccountVaultTokenAccount extends string, TAccountRecipientTokenAccount extends string, TAccountTokenProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ExecuteTokenTransferInput<TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountVaultTokenAccount, TAccountRecipientTokenAccount, TAccountTokenProgram>, config?: { programAddress?: TProgramAddress } ): ExecuteTokenTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountVaultTokenAccount, TAccountRecipientTokenAccount, TAccountTokenProgram> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, agent: { value: input.agent ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: false }, vaultTokenAccount: { value: input.vaultTokenAccount ?? null, isWritable: true }, recipientTokenAccount: { value: input.recipientTokenAccount ?? null, isWritable: true }, tokenProgram: { value: input.tokenProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.tokenProgram.value) {
accounts.tokenProgram.value = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("agent", accounts.agent), getAccountMeta("authority", accounts.authority), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("vaultTokenAccount", accounts.vaultTokenAccount), getAccountMeta("recipientTokenAccount", accounts.recipientTokenAccount), getAccountMeta("tokenProgram", accounts.tokenProgram)], data: getExecuteTokenTransferInstructionDataEncoder().encode(args as ExecuteTokenTransferInstructionDataArgs), programAddress } as ExecuteTokenTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountVaultTokenAccount, TAccountRecipientTokenAccount, TAccountTokenProgram>);
}

export type ParsedExecuteTokenTransferInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * ADR-093: Seeds use the externally-known `authority` account key rather
 * than `vault.authority` read from stored account data. The `has_one`
 * constraint verifies that `authority` matches `vault.authority`.
 */
vault: TAccountMetas[0];
/** The signer must be the agent authority or the vault authority. */
agent: TAccountMetas[1];
/**
 * The vault authority — used as a canonical PDA seed (ADR-093).
 * Not required to be a signer; the `agent` field carries the signature.
 */
authority: TAccountMetas[2];
/**
 * ADR-095: The agent's Registry profile. Read-only cross-program account.
 * Suspension check is done in the instruction handler.
 *
 * `authority.key()` and `vault.profile_nonce`. The suspension check
 * is done explicitly in the instruction handler using `AgentStatus`.
 */
agentProfile: TAccountMetas[3];
/** The vault's token account (source of SPL tokens). Must be owned by the vault PDA. */
vaultTokenAccount: TAccountMetas[4];
/**
 * The recipient's token account.
 *
 * Constraints:
 * - Mint must match `vault_token_account.mint` (already-mitigated per
 * DEEP-AUDIT-2026-04-22 Audit 1 finding SEC-6 — retained as-is).
 * - ADR-072 (SEC-6): The recipient account must NOT be the vault's own
 * token account (the exact same account) AND its SPL owner must not
 * be the vault PDA. This blocks a self-transfer loop that would
 * otherwise let a griefer (or a compromised `agent_identity`) burn
 * rate-limit slots and exhaust the hourly window during incident
 * response — every self-transfer is a no-op at the token-program
 * layer but still increments `txs_in_current_window`.
 *
 * Note: the recipient-mint match was already present pre-ADR-072; the
 * recipient-owner / self-account checks are the net-new SEC-6 fix.
 */
recipientTokenAccount: TAccountMetas[5];
tokenProgram: TAccountMetas[6];
};
data: ExecuteTokenTransferInstructionData; };

export function parseExecuteTokenTransferInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedExecuteTokenTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 7 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { vault: getNextAccount(), agent: getNextAccount(), authority: getNextAccount(), agentProfile: getNextAccount(), vaultTokenAccount: getNextAccount(), recipientTokenAccount: getNextAccount(), tokenProgram: getNextAccount() }, data: getExecuteTokenTransferInstructionDataDecoder().decode(instruction.data) };
}