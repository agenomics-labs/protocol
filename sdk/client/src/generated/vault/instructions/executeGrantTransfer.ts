/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { AGENT_VAULT_PROGRAM_ADDRESS } from '../programs/index.js';

export const EXECUTE_GRANT_TRANSFER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([166, 95, 191, 75, 67, 239, 18, 27]);

export function getExecuteGrantTransferDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(EXECUTE_GRANT_TRANSFER_DISCRIMINATOR); }

export type ExecuteGrantTransferInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountVault extends string | AccountMeta<string> = string, TAccountGrant extends string | AccountMeta<string> = string, TAccountGrantee extends string | AccountMeta<string> = string, TAccountAgentProfile extends string | AccountMeta<string> = string, TAccountRecipient extends string | AccountMeta<string> = string, TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault, TAccountGrant extends string ? WritableAccount<TAccountGrant> : TAccountGrant, TAccountGrantee extends string ? ReadonlySignerAccount<TAccountGrantee> & AccountSignerMeta<TAccountGrantee> : TAccountGrantee, TAccountAgentProfile extends string ? ReadonlyAccount<TAccountAgentProfile> : TAccountAgentProfile, TAccountRecipient extends string ? WritableAccount<TAccountRecipient> : TAccountRecipient, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type ExecuteGrantTransferInstructionData = { discriminator: ReadonlyUint8Array; amountLamports: bigint;  };

export type ExecuteGrantTransferInstructionDataArgs = { amountLamports: number | bigint;  };

export function getExecuteGrantTransferInstructionDataEncoder(): FixedSizeEncoder<ExecuteGrantTransferInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['amountLamports', getU64Encoder()]]), (value) => ({ ...value, discriminator: EXECUTE_GRANT_TRANSFER_DISCRIMINATOR }));
}

export function getExecuteGrantTransferInstructionDataDecoder(): FixedSizeDecoder<ExecuteGrantTransferInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['amountLamports', getU64Decoder()]]);
}

export function getExecuteGrantTransferInstructionDataCodec(): FixedSizeCodec<ExecuteGrantTransferInstructionDataArgs, ExecuteGrantTransferInstructionData> {
    return combineCodec(getExecuteGrantTransferInstructionDataEncoder(), getExecuteGrantTransferInstructionDataDecoder());
}

export type ExecuteGrantTransferInput<TAccountVault extends string = string, TAccountGrant extends string = string, TAccountGrantee extends string = string, TAccountAgentProfile extends string = string, TAccountRecipient extends string = string, TAccountSystemProgram extends string = string> =  {
  vault: Address<TAccountVault>;
grant: Address<TAccountGrant>;
/**
 * The grantee MUST sign — grant.grantee is the canonical authority
 * for `execute_grant_*` and the `constraint` above pins the field
 * against the signer key.
 */
grantee: TransactionSigner<TAccountGrantee>;
/**
 * ADR-095: suspension gate. The PDA is derived from
 * `vault.authority` (canonical seeds) + `vault.profile_nonce`.
 * suspension status is asserted explicitly in the handler.
 */
agentProfile: Address<TAccountAgentProfile>;
/** in the handler. */
recipient: Address<TAccountRecipient>;
systemProgram?: Address<TAccountSystemProgram>;
amountLamports: ExecuteGrantTransferInstructionDataArgs["amountLamports"];
}

export function getExecuteGrantTransferInstruction<TAccountVault extends string, TAccountGrant extends string, TAccountGrantee extends string, TAccountAgentProfile extends string, TAccountRecipient extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ExecuteGrantTransferInput<TAccountVault, TAccountGrant, TAccountGrantee, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): ExecuteGrantTransferInstruction<TProgramAddress, TAccountVault, TAccountGrant, TAccountGrantee, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, grant: { value: input.grant ?? null, isWritable: true }, grantee: { value: input.grantee ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: false }, recipient: { value: input.recipient ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("grant", accounts.grant), getAccountMeta("grantee", accounts.grantee), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("recipient", accounts.recipient), getAccountMeta("systemProgram", accounts.systemProgram)], data: getExecuteGrantTransferInstructionDataEncoder().encode(args as ExecuteGrantTransferInstructionDataArgs), programAddress } as ExecuteGrantTransferInstruction<TProgramAddress, TAccountVault, TAccountGrant, TAccountGrantee, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>);
}

export type ParsedExecuteGrantTransferInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
vault: TAccountMetas[0];
grant: TAccountMetas[1];
/**
 * The grantee MUST sign — grant.grantee is the canonical authority
 * for `execute_grant_*` and the `constraint` above pins the field
 * against the signer key.
 */
grantee: TAccountMetas[2];
/**
 * ADR-095: suspension gate. The PDA is derived from
 * `vault.authority` (canonical seeds) + `vault.profile_nonce`.
 * suspension status is asserted explicitly in the handler.
 */
agentProfile: TAccountMetas[3];
/** in the handler. */
recipient: TAccountMetas[4];
systemProgram: TAccountMetas[5];
};
data: ExecuteGrantTransferInstructionData; };

export function parseExecuteGrantTransferInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedExecuteGrantTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 6 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { vault: getNextAccount(), grant: getNextAccount(), grantee: getNextAccount(), agentProfile: getNextAccount(), recipient: getNextAccount(), systemProgram: getNextAccount() }, data: getExecuteGrantTransferInstructionDataDecoder().decode(instruction.data) };
}