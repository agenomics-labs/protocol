/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findVaultPda } from '../pdas/index.js';
import { AGENT_VAULT_PROGRAM_ADDRESS } from '../programs/index.js';

export const UPDATE_AGENT_IDENTITY_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([133, 153, 86, 40, 165, 20, 149, 42]);

export function getUpdateAgentIdentityDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_AGENT_IDENTITY_DISCRIMINATOR); }

export type UpdateAgentIdentityInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountVault extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountInstructionsSysvar extends string | AccountMeta<string> = "Sysvar1nstructions1111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault, TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountInstructionsSysvar extends string ? ReadonlyAccount<TAccountInstructionsSysvar> : TAccountInstructionsSysvar, ...TRemainingAccounts]>;

export type UpdateAgentIdentityInstructionData = { discriminator: ReadonlyUint8Array; newAgentIdentity: Address; newAgentIdentitySignature: ReadonlyUint8Array;  };

export type UpdateAgentIdentityInstructionDataArgs = { newAgentIdentity: Address; newAgentIdentitySignature: ReadonlyUint8Array;  };

export function getUpdateAgentIdentityInstructionDataEncoder(): FixedSizeEncoder<UpdateAgentIdentityInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['newAgentIdentity', getAddressEncoder()], ['newAgentIdentitySignature', fixEncoderSize(getBytesEncoder(), 64)]]), (value) => ({ ...value, discriminator: UPDATE_AGENT_IDENTITY_DISCRIMINATOR }));
}

export function getUpdateAgentIdentityInstructionDataDecoder(): FixedSizeDecoder<UpdateAgentIdentityInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['newAgentIdentity', getAddressDecoder()], ['newAgentIdentitySignature', fixDecoderSize(getBytesDecoder(), 64)]]);
}

export function getUpdateAgentIdentityInstructionDataCodec(): FixedSizeCodec<UpdateAgentIdentityInstructionDataArgs, UpdateAgentIdentityInstructionData> {
    return combineCodec(getUpdateAgentIdentityInstructionDataEncoder(), getUpdateAgentIdentityInstructionDataDecoder());
}

export type UpdateAgentIdentityAsyncInput<TAccountVault extends string = string, TAccountAuthority extends string = string, TAccountInstructionsSysvar extends string = string> =  {
  vault?: Address<TAccountVault>;
authority: TransactionSigner<TAccountAuthority>;
/**
 * AUD-200 / ADR-124 (path-a, symmetric): Instructions sysvar — read-only,
 * address-pinned to the canonical sysvar pubkey. Used by
 * `identity_bind::verify_ed25519_precompile` to introspect the paired
 * `Ed25519Program::verify` instruction in the same transaction. The
 * caller MUST include an ed25519-program sig-verify instruction
 * covering `vault_identity_bind_message(authority, new_agent_identity)`
 * signed by `new_agent_identity` — this proves control of the candidate
 * hot key at rotation time, closing the AUD-200 mis-bind seam at
 * rotation that mirrors AUD-116 at init.
 * canonical sysvar account satisfies the constraint.
 */
instructionsSysvar?: Address<TAccountInstructionsSysvar>;
newAgentIdentity: UpdateAgentIdentityInstructionDataArgs["newAgentIdentity"];
newAgentIdentitySignature: UpdateAgentIdentityInstructionDataArgs["newAgentIdentitySignature"];
}

export async function getUpdateAgentIdentityInstructionAsync<TAccountVault extends string, TAccountAuthority extends string, TAccountInstructionsSysvar extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: UpdateAgentIdentityAsyncInput<TAccountVault, TAccountAuthority, TAccountInstructionsSysvar>, config?: { programAddress?: TProgramAddress } ): Promise<UpdateAgentIdentityInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountInstructionsSysvar>> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false }, instructionsSysvar: { value: input.instructionsSysvar ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.vault.value) {
accounts.vault.value = await findVaultPda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}
if (!accounts.instructionsSysvar.value) {
accounts.instructionsSysvar.value = 'Sysvar1nstructions1111111111111111111111111' as Address<'Sysvar1nstructions1111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("authority", accounts.authority), getAccountMeta("instructionsSysvar", accounts.instructionsSysvar)], data: getUpdateAgentIdentityInstructionDataEncoder().encode(args as UpdateAgentIdentityInstructionDataArgs), programAddress } as UpdateAgentIdentityInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountInstructionsSysvar>);
}

export type UpdateAgentIdentityInput<TAccountVault extends string = string, TAccountAuthority extends string = string, TAccountInstructionsSysvar extends string = string> =  {
  vault: Address<TAccountVault>;
authority: TransactionSigner<TAccountAuthority>;
/**
 * AUD-200 / ADR-124 (path-a, symmetric): Instructions sysvar — read-only,
 * address-pinned to the canonical sysvar pubkey. Used by
 * `identity_bind::verify_ed25519_precompile` to introspect the paired
 * `Ed25519Program::verify` instruction in the same transaction. The
 * caller MUST include an ed25519-program sig-verify instruction
 * covering `vault_identity_bind_message(authority, new_agent_identity)`
 * signed by `new_agent_identity` — this proves control of the candidate
 * hot key at rotation time, closing the AUD-200 mis-bind seam at
 * rotation that mirrors AUD-116 at init.
 * canonical sysvar account satisfies the constraint.
 */
instructionsSysvar?: Address<TAccountInstructionsSysvar>;
newAgentIdentity: UpdateAgentIdentityInstructionDataArgs["newAgentIdentity"];
newAgentIdentitySignature: UpdateAgentIdentityInstructionDataArgs["newAgentIdentitySignature"];
}

export function getUpdateAgentIdentityInstruction<TAccountVault extends string, TAccountAuthority extends string, TAccountInstructionsSysvar extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: UpdateAgentIdentityInput<TAccountVault, TAccountAuthority, TAccountInstructionsSysvar>, config?: { programAddress?: TProgramAddress } ): UpdateAgentIdentityInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountInstructionsSysvar> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false }, instructionsSysvar: { value: input.instructionsSysvar ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.instructionsSysvar.value) {
accounts.instructionsSysvar.value = 'Sysvar1nstructions1111111111111111111111111' as Address<'Sysvar1nstructions1111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("authority", accounts.authority), getAccountMeta("instructionsSysvar", accounts.instructionsSysvar)], data: getUpdateAgentIdentityInstructionDataEncoder().encode(args as UpdateAgentIdentityInstructionDataArgs), programAddress } as UpdateAgentIdentityInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountInstructionsSysvar>);
}

export type ParsedUpdateAgentIdentityInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
vault: TAccountMetas[0];
authority: TAccountMetas[1];
/**
 * AUD-200 / ADR-124 (path-a, symmetric): Instructions sysvar — read-only,
 * address-pinned to the canonical sysvar pubkey. Used by
 * `identity_bind::verify_ed25519_precompile` to introspect the paired
 * `Ed25519Program::verify` instruction in the same transaction. The
 * caller MUST include an ed25519-program sig-verify instruction
 * covering `vault_identity_bind_message(authority, new_agent_identity)`
 * signed by `new_agent_identity` — this proves control of the candidate
 * hot key at rotation time, closing the AUD-200 mis-bind seam at
 * rotation that mirrors AUD-116 at init.
 * canonical sysvar account satisfies the constraint.
 */
instructionsSysvar: TAccountMetas[2];
};
data: UpdateAgentIdentityInstructionData; };

export function parseUpdateAgentIdentityInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedUpdateAgentIdentityInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 3 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { vault: getNextAccount(), authority: getNextAccount(), instructionsSysvar: getNextAccount() }, data: getUpdateAgentIdentityInstructionDataDecoder().decode(instruction.data) };
}