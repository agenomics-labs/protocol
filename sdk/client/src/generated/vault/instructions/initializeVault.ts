/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount, type WritableSignerAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findVaultPda } from '../pdas/index.js';
import { AGENT_VAULT_PROGRAM_ADDRESS } from '../programs/index.js';

export const INITIALIZE_VAULT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([48, 191, 163, 44, 71, 129, 63, 164]);

export function getInitializeVaultDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(INITIALIZE_VAULT_DISCRIMINATOR); }

export type InitializeVaultInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountVault extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountOwnerNonce extends string | AccountMeta<string> = string, TAccountInstructionsSysvar extends string | AccountMeta<string> = "Sysvar1nstructions1111111111111111111111111", TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault, TAccountAuthority extends string ? WritableSignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountOwnerNonce extends string ? ReadonlyAccount<TAccountOwnerNonce> : TAccountOwnerNonce, TAccountInstructionsSysvar extends string ? ReadonlyAccount<TAccountInstructionsSysvar> : TAccountInstructionsSysvar, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type InitializeVaultInstructionData = { discriminator: ReadonlyUint8Array; agentIdentity: Address; dailyLimitLamports: bigint; perTxLimitLamports: bigint; maxTxsPerHour: number; agentIdentitySignature: ReadonlyUint8Array;  };

export type InitializeVaultInstructionDataArgs = { agentIdentity: Address; dailyLimitLamports: number | bigint; perTxLimitLamports: number | bigint; maxTxsPerHour: number; agentIdentitySignature: ReadonlyUint8Array;  };

export function getInitializeVaultInstructionDataEncoder(): FixedSizeEncoder<InitializeVaultInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['agentIdentity', getAddressEncoder()], ['dailyLimitLamports', getU64Encoder()], ['perTxLimitLamports', getU64Encoder()], ['maxTxsPerHour', getU32Encoder()], ['agentIdentitySignature', fixEncoderSize(getBytesEncoder(), 64)]]), (value) => ({ ...value, discriminator: INITIALIZE_VAULT_DISCRIMINATOR }));
}

export function getInitializeVaultInstructionDataDecoder(): FixedSizeDecoder<InitializeVaultInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['agentIdentity', getAddressDecoder()], ['dailyLimitLamports', getU64Decoder()], ['perTxLimitLamports', getU64Decoder()], ['maxTxsPerHour', getU32Decoder()], ['agentIdentitySignature', fixDecoderSize(getBytesDecoder(), 64)]]);
}

export function getInitializeVaultInstructionDataCodec(): FixedSizeCodec<InitializeVaultInstructionDataArgs, InitializeVaultInstructionData> {
    return combineCodec(getInitializeVaultInstructionDataEncoder(), getInitializeVaultInstructionDataDecoder());
}

export type InitializeVaultAsyncInput<TAccountVault extends string = string, TAccountAuthority extends string = string, TAccountOwnerNonce extends string = string, TAccountInstructionsSysvar extends string = string, TAccountSystemProgram extends string = string> =  {
  vault?: Address<TAccountVault>;
authority: TransactionSigner<TAccountAuthority>;
/**
 * AUD-008 / ADR-097: MUST exist — vault initialization requires prior
 * agent registration. Sources `vault.profile_nonce` from the Registry's
 * authoritative `OwnerNonce` account, eliminating the user-supplied
 * scalar that could brick transfers.
 *
 * The seeds constraint `[authority.key().as_ref(), b"owner-nonce"]`
 * under `agent_registry::ID` simultaneously:
 * 1. Enforces existence (Anchor fails to deserialize if the account is
 * not initialized, surfacing `AccountNotInitialized`).
 * 2. Closes cross-account reuse: an attacker cannot pass another
 * user's `OwnerNonce` account because PDA derivation is keyed on
 * `authority.key()`. Substituting a foreign account fails
 * `ConstraintSeeds`.
 *
 * `OwnerNonce` has only `nonce: u64`; a field-level
 * `owner_nonce.authority == authority.key()` check is unavailable and
 * unnecessary because the seeds derivation already binds the account
 * to `authority`.
 */
ownerNonce?: Address<TAccountOwnerNonce>;
/**
 * ADR-124 / AUD-116 (path-a): Instructions sysvar — read-only,
 * address-pinned to the canonical sysvar pubkey. Used by
 * `identity_bind::verify_ed25519_precompile` to introspect the paired
 * `Ed25519Program::verify` instruction in the same transaction. The
 * caller MUST include an ed25519-program sig-verify instruction
 * covering `vault_identity_bind_message(authority, agent_identity)`
 * signed by `agent_identity` — this proves control of the candidate
 * hot key at init time, closing the AUD-116 init-mis-bind seam.
 * canonical sysvar account satisfies the constraint.
 */
instructionsSysvar?: Address<TAccountInstructionsSysvar>;
systemProgram?: Address<TAccountSystemProgram>;
agentIdentity: InitializeVaultInstructionDataArgs["agentIdentity"];
dailyLimitLamports: InitializeVaultInstructionDataArgs["dailyLimitLamports"];
perTxLimitLamports: InitializeVaultInstructionDataArgs["perTxLimitLamports"];
maxTxsPerHour: InitializeVaultInstructionDataArgs["maxTxsPerHour"];
agentIdentitySignature: InitializeVaultInstructionDataArgs["agentIdentitySignature"];
}

export async function getInitializeVaultInstructionAsync<TAccountVault extends string, TAccountAuthority extends string, TAccountOwnerNonce extends string, TAccountInstructionsSysvar extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: InitializeVaultAsyncInput<TAccountVault, TAccountAuthority, TAccountOwnerNonce, TAccountInstructionsSysvar, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): Promise<InitializeVaultInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce, TAccountInstructionsSysvar, TAccountSystemProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true }, ownerNonce: { value: input.ownerNonce ?? null, isWritable: false }, instructionsSysvar: { value: input.instructionsSysvar ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.vault.value) {
accounts.vault.value = await findVaultPda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}
if (!accounts.ownerNonce.value) {
accounts.ownerNonce.value = await getProgramDerivedAddress({ programAddress: '26KETQPxeMmbakxpVbUEpQBQmVgpabHAweTHBRgBHjW7' as Address<'26KETQPxeMmbakxpVbUEpQBQmVgpabHAweTHBRgBHjW7'>, seeds: [getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("authority", accounts.authority.value)), getBytesEncoder().encode(new Uint8Array([111, 119, 110, 101, 114, 45, 110, 111, 110, 99, 101]))] });
}
if (!accounts.instructionsSysvar.value) {
accounts.instructionsSysvar.value = 'Sysvar1nstructions1111111111111111111111111' as Address<'Sysvar1nstructions1111111111111111111111111'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("authority", accounts.authority), getAccountMeta("ownerNonce", accounts.ownerNonce), getAccountMeta("instructionsSysvar", accounts.instructionsSysvar), getAccountMeta("systemProgram", accounts.systemProgram)], data: getInitializeVaultInstructionDataEncoder().encode(args as InitializeVaultInstructionDataArgs), programAddress } as InitializeVaultInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce, TAccountInstructionsSysvar, TAccountSystemProgram>);
}

export type InitializeVaultInput<TAccountVault extends string = string, TAccountAuthority extends string = string, TAccountOwnerNonce extends string = string, TAccountInstructionsSysvar extends string = string, TAccountSystemProgram extends string = string> =  {
  vault: Address<TAccountVault>;
authority: TransactionSigner<TAccountAuthority>;
/**
 * AUD-008 / ADR-097: MUST exist — vault initialization requires prior
 * agent registration. Sources `vault.profile_nonce` from the Registry's
 * authoritative `OwnerNonce` account, eliminating the user-supplied
 * scalar that could brick transfers.
 *
 * The seeds constraint `[authority.key().as_ref(), b"owner-nonce"]`
 * under `agent_registry::ID` simultaneously:
 * 1. Enforces existence (Anchor fails to deserialize if the account is
 * not initialized, surfacing `AccountNotInitialized`).
 * 2. Closes cross-account reuse: an attacker cannot pass another
 * user's `OwnerNonce` account because PDA derivation is keyed on
 * `authority.key()`. Substituting a foreign account fails
 * `ConstraintSeeds`.
 *
 * `OwnerNonce` has only `nonce: u64`; a field-level
 * `owner_nonce.authority == authority.key()` check is unavailable and
 * unnecessary because the seeds derivation already binds the account
 * to `authority`.
 */
ownerNonce: Address<TAccountOwnerNonce>;
/**
 * ADR-124 / AUD-116 (path-a): Instructions sysvar — read-only,
 * address-pinned to the canonical sysvar pubkey. Used by
 * `identity_bind::verify_ed25519_precompile` to introspect the paired
 * `Ed25519Program::verify` instruction in the same transaction. The
 * caller MUST include an ed25519-program sig-verify instruction
 * covering `vault_identity_bind_message(authority, agent_identity)`
 * signed by `agent_identity` — this proves control of the candidate
 * hot key at init time, closing the AUD-116 init-mis-bind seam.
 * canonical sysvar account satisfies the constraint.
 */
instructionsSysvar?: Address<TAccountInstructionsSysvar>;
systemProgram?: Address<TAccountSystemProgram>;
agentIdentity: InitializeVaultInstructionDataArgs["agentIdentity"];
dailyLimitLamports: InitializeVaultInstructionDataArgs["dailyLimitLamports"];
perTxLimitLamports: InitializeVaultInstructionDataArgs["perTxLimitLamports"];
maxTxsPerHour: InitializeVaultInstructionDataArgs["maxTxsPerHour"];
agentIdentitySignature: InitializeVaultInstructionDataArgs["agentIdentitySignature"];
}

export function getInitializeVaultInstruction<TAccountVault extends string, TAccountAuthority extends string, TAccountOwnerNonce extends string, TAccountInstructionsSysvar extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: InitializeVaultInput<TAccountVault, TAccountAuthority, TAccountOwnerNonce, TAccountInstructionsSysvar, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): InitializeVaultInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce, TAccountInstructionsSysvar, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: true }, ownerNonce: { value: input.ownerNonce ?? null, isWritable: false }, instructionsSysvar: { value: input.instructionsSysvar ?? null, isWritable: false }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.instructionsSysvar.value) {
accounts.instructionsSysvar.value = 'Sysvar1nstructions1111111111111111111111111' as Address<'Sysvar1nstructions1111111111111111111111111'>;
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("authority", accounts.authority), getAccountMeta("ownerNonce", accounts.ownerNonce), getAccountMeta("instructionsSysvar", accounts.instructionsSysvar), getAccountMeta("systemProgram", accounts.systemProgram)], data: getInitializeVaultInstructionDataEncoder().encode(args as InitializeVaultInstructionDataArgs), programAddress } as InitializeVaultInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce, TAccountInstructionsSysvar, TAccountSystemProgram>);
}

export type ParsedInitializeVaultInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
vault: TAccountMetas[0];
authority: TAccountMetas[1];
/**
 * AUD-008 / ADR-097: MUST exist — vault initialization requires prior
 * agent registration. Sources `vault.profile_nonce` from the Registry's
 * authoritative `OwnerNonce` account, eliminating the user-supplied
 * scalar that could brick transfers.
 *
 * The seeds constraint `[authority.key().as_ref(), b"owner-nonce"]`
 * under `agent_registry::ID` simultaneously:
 * 1. Enforces existence (Anchor fails to deserialize if the account is
 * not initialized, surfacing `AccountNotInitialized`).
 * 2. Closes cross-account reuse: an attacker cannot pass another
 * user's `OwnerNonce` account because PDA derivation is keyed on
 * `authority.key()`. Substituting a foreign account fails
 * `ConstraintSeeds`.
 *
 * `OwnerNonce` has only `nonce: u64`; a field-level
 * `owner_nonce.authority == authority.key()` check is unavailable and
 * unnecessary because the seeds derivation already binds the account
 * to `authority`.
 */
ownerNonce: TAccountMetas[2];
/**
 * ADR-124 / AUD-116 (path-a): Instructions sysvar — read-only,
 * address-pinned to the canonical sysvar pubkey. Used by
 * `identity_bind::verify_ed25519_precompile` to introspect the paired
 * `Ed25519Program::verify` instruction in the same transaction. The
 * caller MUST include an ed25519-program sig-verify instruction
 * covering `vault_identity_bind_message(authority, agent_identity)`
 * signed by `agent_identity` — this proves control of the candidate
 * hot key at init time, closing the AUD-116 init-mis-bind seam.
 * canonical sysvar account satisfies the constraint.
 */
instructionsSysvar: TAccountMetas[3];
systemProgram: TAccountMetas[4];
};
data: InitializeVaultInstructionData; };

export function parseInitializeVaultInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedInitializeVaultInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 5 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { vault: getNextAccount(), authority: getNextAccount(), ownerNonce: getNextAccount(), instructionsSysvar: getNextAccount(), systemProgram: getNextAccount() }, data: getInitializeVaultInstructionDataDecoder().decode(instruction.data) };
}