/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findVaultPda } from '../pdas/index.js';
import { AGENT_VAULT_PROGRAM_ADDRESS } from '../programs/index.js';

export const EXECUTE_TRANSFER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([233, 126, 160, 184, 235, 206, 31, 119]);

export function getExecuteTransferDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(EXECUTE_TRANSFER_DISCRIMINATOR); }

export type ExecuteTransferInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountVault extends string | AccountMeta<string> = string, TAccountAgent extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountAgentProfile extends string | AccountMeta<string> = string, TAccountRecipient extends string | AccountMeta<string> = string, TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111", TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault, TAccountAgent extends string ? ReadonlySignerAccount<TAccountAgent> & AccountSignerMeta<TAccountAgent> : TAccountAgent, TAccountAuthority extends string ? ReadonlyAccount<TAccountAuthority> : TAccountAuthority, TAccountAgentProfile extends string ? ReadonlyAccount<TAccountAgentProfile> : TAccountAgentProfile, TAccountRecipient extends string ? WritableAccount<TAccountRecipient> : TAccountRecipient, TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram, ...TRemainingAccounts]>;

export type ExecuteTransferInstructionData = { discriminator: ReadonlyUint8Array; amountLamports: bigint; toolIdHash: ReadonlyUint8Array;  };

export type ExecuteTransferInstructionDataArgs = { amountLamports: number | bigint; toolIdHash: ReadonlyUint8Array;  };

export function getExecuteTransferInstructionDataEncoder(): FixedSizeEncoder<ExecuteTransferInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['amountLamports', getU64Encoder()], ['toolIdHash', fixEncoderSize(getBytesEncoder(), 32)]]), (value) => ({ ...value, discriminator: EXECUTE_TRANSFER_DISCRIMINATOR }));
}

export function getExecuteTransferInstructionDataDecoder(): FixedSizeDecoder<ExecuteTransferInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['amountLamports', getU64Decoder()], ['toolIdHash', fixDecoderSize(getBytesDecoder(), 32)]]);
}

export function getExecuteTransferInstructionDataCodec(): FixedSizeCodec<ExecuteTransferInstructionDataArgs, ExecuteTransferInstructionData> {
    return combineCodec(getExecuteTransferInstructionDataEncoder(), getExecuteTransferInstructionDataDecoder());
}

export type ExecuteTransferAsyncInput<TAccountVault extends string = string, TAccountAgent extends string = string, TAccountAuthority extends string = string, TAccountAgentProfile extends string = string, TAccountRecipient extends string = string, TAccountSystemProgram extends string = string> =  {
  /**
 * The vault PDA — serves as both state account and SOL source.
 * ADR-029: Removed vestigial vault_account field; the vault PDA itself
 * holds SOL and is used directly for lamport transfers.
 *
 * ADR-093: Seeds use the externally-known `authority` account key rather
 * than `vault.authority` read from stored account data. This eliminates
 * the self-referential pattern (needing to load the vault to find the
 * seeds needed to find the vault). The `has_one` constraint verifies
 * that `authority` matches `vault.authority`.
 */
vault?: Address<TAccountVault>;
/** The signer must be the agent authority or the vault authority. */
agent: TransactionSigner<TAccountAgent>;
/**
 * The vault authority — used as a canonical PDA seed (ADR-093).
 * Not required to be a signer; the `agent` field carries the signature.
 */
authority: Address<TAccountAuthority>;
/**
 * ADR-095: The agent's Registry profile. Read-only cross-program account.
 * The constraint enforces that the agent is not suspended. The PDA seed
 * uses `vault.profile_nonce` (recorded at `initialize_vault` time) so
 * that the derivation matches the live profile PDA (ADR-097).
 *
 * `authority.key()` and `vault.profile_nonce`. The suspension check
 * is done explicitly in the instruction handler using `AgentStatus`.
 */
agentProfile: Address<TAccountAgentProfile>;
recipient: Address<TAccountRecipient>;
systemProgram?: Address<TAccountSystemProgram>;
amountLamports: ExecuteTransferInstructionDataArgs["amountLamports"];
toolIdHash: ExecuteTransferInstructionDataArgs["toolIdHash"];
}

export async function getExecuteTransferInstructionAsync<TAccountVault extends string, TAccountAgent extends string, TAccountAuthority extends string, TAccountAgentProfile extends string, TAccountRecipient extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ExecuteTransferAsyncInput<TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): Promise<ExecuteTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, agent: { value: input.agent ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: false }, recipient: { value: input.recipient ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.vault.value) {
accounts.vault.value = await findVaultPda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("agent", accounts.agent), getAccountMeta("authority", accounts.authority), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("recipient", accounts.recipient), getAccountMeta("systemProgram", accounts.systemProgram)], data: getExecuteTransferInstructionDataEncoder().encode(args as ExecuteTransferInstructionDataArgs), programAddress } as ExecuteTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>);
}

export type ExecuteTransferInput<TAccountVault extends string = string, TAccountAgent extends string = string, TAccountAuthority extends string = string, TAccountAgentProfile extends string = string, TAccountRecipient extends string = string, TAccountSystemProgram extends string = string> =  {
  /**
 * The vault PDA — serves as both state account and SOL source.
 * ADR-029: Removed vestigial vault_account field; the vault PDA itself
 * holds SOL and is used directly for lamport transfers.
 *
 * ADR-093: Seeds use the externally-known `authority` account key rather
 * than `vault.authority` read from stored account data. This eliminates
 * the self-referential pattern (needing to load the vault to find the
 * seeds needed to find the vault). The `has_one` constraint verifies
 * that `authority` matches `vault.authority`.
 */
vault: Address<TAccountVault>;
/** The signer must be the agent authority or the vault authority. */
agent: TransactionSigner<TAccountAgent>;
/**
 * The vault authority — used as a canonical PDA seed (ADR-093).
 * Not required to be a signer; the `agent` field carries the signature.
 */
authority: Address<TAccountAuthority>;
/**
 * ADR-095: The agent's Registry profile. Read-only cross-program account.
 * The constraint enforces that the agent is not suspended. The PDA seed
 * uses `vault.profile_nonce` (recorded at `initialize_vault` time) so
 * that the derivation matches the live profile PDA (ADR-097).
 *
 * `authority.key()` and `vault.profile_nonce`. The suspension check
 * is done explicitly in the instruction handler using `AgentStatus`.
 */
agentProfile: Address<TAccountAgentProfile>;
recipient: Address<TAccountRecipient>;
systemProgram?: Address<TAccountSystemProgram>;
amountLamports: ExecuteTransferInstructionDataArgs["amountLamports"];
toolIdHash: ExecuteTransferInstructionDataArgs["toolIdHash"];
}

export function getExecuteTransferInstruction<TAccountVault extends string, TAccountAgent extends string, TAccountAuthority extends string, TAccountAgentProfile extends string, TAccountRecipient extends string, TAccountSystemProgram extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ExecuteTransferInput<TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>, config?: { programAddress?: TProgramAddress } ): ExecuteTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, agent: { value: input.agent ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: false }, recipient: { value: input.recipient ?? null, isWritable: true }, systemProgram: { value: input.systemProgram ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.systemProgram.value) {
accounts.systemProgram.value = '11111111111111111111111111111111' as Address<'11111111111111111111111111111111'>;
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("agent", accounts.agent), getAccountMeta("authority", accounts.authority), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("recipient", accounts.recipient), getAccountMeta("systemProgram", accounts.systemProgram)], data: getExecuteTransferInstructionDataEncoder().encode(args as ExecuteTransferInstructionDataArgs), programAddress } as ExecuteTransferInstruction<TProgramAddress, TAccountVault, TAccountAgent, TAccountAuthority, TAccountAgentProfile, TAccountRecipient, TAccountSystemProgram>);
}

export type ParsedExecuteTransferInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * The vault PDA — serves as both state account and SOL source.
 * ADR-029: Removed vestigial vault_account field; the vault PDA itself
 * holds SOL and is used directly for lamport transfers.
 *
 * ADR-093: Seeds use the externally-known `authority` account key rather
 * than `vault.authority` read from stored account data. This eliminates
 * the self-referential pattern (needing to load the vault to find the
 * seeds needed to find the vault). The `has_one` constraint verifies
 * that `authority` matches `vault.authority`.
 */
vault: TAccountMetas[0];
/** The signer must be the agent authority or the vault authority. */
agent: TAccountMetas[1];
/**
 * The vault authority — used as a canonical PDA seed (ADR-093).
 * Not required to be a signer; the `agent` field carries the signature.
 */
authority: TAccountMetas[2];
/**
 * ADR-095: The agent's Registry profile. Read-only cross-program account.
 * The constraint enforces that the agent is not suspended. The PDA seed
 * uses `vault.profile_nonce` (recorded at `initialize_vault` time) so
 * that the derivation matches the live profile PDA (ADR-097).
 *
 * `authority.key()` and `vault.profile_nonce`. The suspension check
 * is done explicitly in the instruction handler using `AgentStatus`.
 */
agentProfile: TAccountMetas[3];
recipient: TAccountMetas[4];
systemProgram: TAccountMetas[5];
};
data: ExecuteTransferInstructionData; };

export function parseExecuteTransferInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedExecuteTransferInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 6 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { vault: getNextAccount(), agent: getNextAccount(), authority: getNextAccount(), agentProfile: getNextAccount(), recipient: getNextAccount(), systemProgram: getNextAccount() }, data: getExecuteTransferInstructionDataDecoder().decode(instruction.data) };
}