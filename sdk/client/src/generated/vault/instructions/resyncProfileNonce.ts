/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findVaultPda } from '../pdas/index.js';
import { AGENT_VAULT_PROGRAM_ADDRESS } from '../programs/index.js';

export const RESYNC_PROFILE_NONCE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([162, 155, 63, 147, 159, 89, 249, 183]);

export function getResyncProfileNonceDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(RESYNC_PROFILE_NONCE_DISCRIMINATOR); }

export type ResyncProfileNonceInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountVault extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TAccountOwnerNonce extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountVault extends string ? WritableAccount<TAccountVault> : TAccountVault, TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority> : TAccountAuthority, TAccountOwnerNonce extends string ? ReadonlyAccount<TAccountOwnerNonce> : TAccountOwnerNonce, ...TRemainingAccounts]>;

export type ResyncProfileNonceInstructionData = { discriminator: ReadonlyUint8Array;  };

export type ResyncProfileNonceInstructionDataArgs = {  };

export function getResyncProfileNonceInstructionDataEncoder(): FixedSizeEncoder<ResyncProfileNonceInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: RESYNC_PROFILE_NONCE_DISCRIMINATOR }));
}

export function getResyncProfileNonceInstructionDataDecoder(): FixedSizeDecoder<ResyncProfileNonceInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getResyncProfileNonceInstructionDataCodec(): FixedSizeCodec<ResyncProfileNonceInstructionDataArgs, ResyncProfileNonceInstructionData> {
    return combineCodec(getResyncProfileNonceInstructionDataEncoder(), getResyncProfileNonceInstructionDataDecoder());
}

export type ResyncProfileNonceAsyncInput<TAccountVault extends string = string, TAccountAuthority extends string = string, TAccountOwnerNonce extends string = string> =  {
  vault?: Address<TAccountVault>;
authority: TransactionSigner<TAccountAuthority>;
/**
 * The Registry's authoritative `OwnerNonce` PDA for `authority`. Same
 * derivation + program binding as `InitializeVault::owner_nonce`, so
 * register-first and cross-account-reuse guards both still hold.
 */
ownerNonce?: Address<TAccountOwnerNonce>;
}

export async function getResyncProfileNonceInstructionAsync<TAccountVault extends string, TAccountAuthority extends string, TAccountOwnerNonce extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ResyncProfileNonceAsyncInput<TAccountVault, TAccountAuthority, TAccountOwnerNonce>, config?: { programAddress?: TProgramAddress } ): Promise<ResyncProfileNonceInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce>> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false }, ownerNonce: { value: input.ownerNonce ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Resolve default values.
if (!accounts.vault.value) {
accounts.vault.value = await findVaultPda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}
if (!accounts.ownerNonce.value) {
accounts.ownerNonce.value = await getProgramDerivedAddress({ programAddress: '26KETQPxeMmbakxpVbUEpQBQmVgpabHAweTHBRgBHjW7' as Address<'26KETQPxeMmbakxpVbUEpQBQmVgpabHAweTHBRgBHjW7'>, seeds: [getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("authority", accounts.authority.value)), getBytesEncoder().encode(new Uint8Array([111, 119, 110, 101, 114, 45, 110, 111, 110, 99, 101]))] });
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("authority", accounts.authority), getAccountMeta("ownerNonce", accounts.ownerNonce)], data: getResyncProfileNonceInstructionDataEncoder().encode({}), programAddress } as ResyncProfileNonceInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce>);
}

export type ResyncProfileNonceInput<TAccountVault extends string = string, TAccountAuthority extends string = string, TAccountOwnerNonce extends string = string> =  {
  vault: Address<TAccountVault>;
authority: TransactionSigner<TAccountAuthority>;
/**
 * The Registry's authoritative `OwnerNonce` PDA for `authority`. Same
 * derivation + program binding as `InitializeVault::owner_nonce`, so
 * register-first and cross-account-reuse guards both still hold.
 */
ownerNonce: Address<TAccountOwnerNonce>;
}

export function getResyncProfileNonceInstruction<TAccountVault extends string, TAccountAuthority extends string, TAccountOwnerNonce extends string, TProgramAddress extends Address = typeof AGENT_VAULT_PROGRAM_ADDRESS>(input: ResyncProfileNonceInput<TAccountVault, TAccountAuthority, TAccountOwnerNonce>, config?: { programAddress?: TProgramAddress } ): ResyncProfileNonceInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_VAULT_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { vault: { value: input.vault ?? null, isWritable: true }, authority: { value: input.authority ?? null, isWritable: false }, ownerNonce: { value: input.ownerNonce ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("vault", accounts.vault), getAccountMeta("authority", accounts.authority), getAccountMeta("ownerNonce", accounts.ownerNonce)], data: getResyncProfileNonceInstructionDataEncoder().encode({}), programAddress } as ResyncProfileNonceInstruction<TProgramAddress, TAccountVault, TAccountAuthority, TAccountOwnerNonce>);
}

export type ParsedResyncProfileNonceInstruction<TProgram extends string = typeof AGENT_VAULT_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
vault: TAccountMetas[0];
authority: TAccountMetas[1];
/**
 * The Registry's authoritative `OwnerNonce` PDA for `authority`. Same
 * derivation + program binding as `InitializeVault::owner_nonce`, so
 * register-first and cross-account-reuse guards both still hold.
 */
ownerNonce: TAccountMetas[2];
};
data: ResyncProfileNonceInstructionData; };

export function parseResyncProfileNonceInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedResyncProfileNonceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 3 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { vault: getNextAccount(), authority: getNextAccount(), ownerNonce: getNextAccount() }, data: getResyncProfileNonceInstructionDataDecoder().decode(instruction.data) };
}