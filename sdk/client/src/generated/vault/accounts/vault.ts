/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type Codec, type Decoder, type EncodedAccount, type Encoder, type FetchAccountConfig, type FetchAccountsConfig, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getTokenSpendRecordDecoder, getTokenSpendRecordEncoder, getVaultPolicyDecoder, getVaultPolicyEncoder, type TokenSpendRecord, type TokenSpendRecordArgs, type VaultPolicy, type VaultPolicyArgs } from '../types/index.js';

export const VAULT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([211, 8, 232, 43, 2, 152, 117, 119]);

export function getVaultDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(VAULT_DISCRIMINATOR); }

export type Vault = { discriminator: ReadonlyUint8Array; 
/** The agent identity this vault is linked to (for reputation tracking). */
agentIdentity: Address; 
/** The authority that can pause/resume and update policies. */
authority: Address; 
/** When true, no transfers or program calls are permitted. */
paused: boolean; 
/** Cumulative SOL spent today (resets daily). */
spentTodayLamports: bigint; 
/** The day for which spent_today_lamports is tracked (Unix timestamp / 86400). */
lastSpendDay: bigint; 
/** Spending policy for this vault. */
policy: VaultPolicy; 
/** Counter for transactions in the current rate-limit window. */
txsInCurrentWindow: number; 
/** Timestamp of when the current rate-limit window started. */
rateLimitWindowStart: bigint; 
/** Per-token daily spending records (max MAX_TOKEN_SPEND_RECORDS entries). */
tokenSpendRecords: Array<TokenSpendRecord>; 
/** PDA bump seed for vault signing in CPIs. */
bump: number; 
/**
 * ADR-095 / ADR-097: The registration nonce of the agent's current
 * `AgentProfile` PDA at the time this vault was initialized. Used by
 * `execute_transfer` and `execute_token_transfer` to re-derive the
 * profile PDA address for the suspension check. Must match the nonce
 * stamped in `AgentProfile.registration_nonce`.
 */
profileNonce: bigint; 
/**
 * AUD-023 / PR-X: Unix timestamp of the most recent
 * `update_agent_identity` rotation. Used to enforce a sliding-window
 * rotation cap of one rotation per 24h, preventing a compromised
 * authority from rotating to a fresh hot key, draining the daily cap,
 * and rotating again to bypass the daily limit.
 *
 * Initialized to 0 by `initialize_vault`, so the very first rotation
 * always succeeds. Updated to `Clock::get()?.unix_timestamp` on every
 * successful rotation.
 *
 * Migration note: vaults deployed before PR-X have this field
 * implicitly set to 0 (Anchor zero-fills new fields at the end of the
 * account on first deserialization after the upgrade). Their first
 * rotation post-upgrade is therefore unrestricted; subsequent rotations
 * are gated by the 24h window. Bumps the account `space` by 8 bytes.
 */
lastRotationAt: bigint; 
/**
 * ADR-138: monotonically-increasing counter incremented on every
 * successful `update_policy` call. Stamped into every
 * `ExecutionAttested` event so off-chain consumers can pin which
 * policy version was in force when a value-moving action executed.
 *
 * Initialized to 0 by `initialize_vault`. Bumped via
 * `checked_add(1)` in `update_policy`; the protocol does NOT support
 * rollback, so the field is strictly increasing across the vault's
 * lifetime.
 *
 * Migration note: vaults deployed before ADR-138 have this field
 * implicitly set to 0 on first post-upgrade deserialization (Anchor
 * zero-fills new trailing fields). Their first `update_policy` after
 * the upgrade lands at version 1; subsequent actions are stamped
 * with whatever the cumulative version is. Bumps the account `space`
 * by 4 bytes.
 */
policyVersion: number; 
/**
 * ADR-111: Count of `DelegationGrant` PDAs currently outstanding for
 * this vault. Bumped on `create_delegation_grant`, decremented on
 * `revoke_delegation_grant`. The vault rejects creation of a new
 * grant once this reaches `MAX_ACTIVE_GRANTS_PER_VAULT` (32).
 *
 * Migration note: vaults deployed before ADR-111 have this field
 * implicitly set to 0 (Anchor zero-fills new fields appended at the
 * end of the account on first post-upgrade deserialization). Bumps
 * the account `space` by 1 byte; the existing 200-byte safety margin
 * in the vault allocation absorbs it without a layout change.
 *
 * Ordering note: this field MUST stay after `policy_version` (ADR-138)
 * so the Anchor zero-fill semantics work for vaults deployed with the
 * ADR-138 upgrade but not yet the ADR-111 one. New trailing fields go
 * at the end of the struct in chronological merge order.
 */
activeGrantCount: number;  };

export type VaultArgs = { 
/** The agent identity this vault is linked to (for reputation tracking). */
agentIdentity: Address; 
/** The authority that can pause/resume and update policies. */
authority: Address; 
/** When true, no transfers or program calls are permitted. */
paused: boolean; 
/** Cumulative SOL spent today (resets daily). */
spentTodayLamports: number | bigint; 
/** The day for which spent_today_lamports is tracked (Unix timestamp / 86400). */
lastSpendDay: number | bigint; 
/** Spending policy for this vault. */
policy: VaultPolicyArgs; 
/** Counter for transactions in the current rate-limit window. */
txsInCurrentWindow: number; 
/** Timestamp of when the current rate-limit window started. */
rateLimitWindowStart: number | bigint; 
/** Per-token daily spending records (max MAX_TOKEN_SPEND_RECORDS entries). */
tokenSpendRecords: Array<TokenSpendRecordArgs>; 
/** PDA bump seed for vault signing in CPIs. */
bump: number; 
/**
 * ADR-095 / ADR-097: The registration nonce of the agent's current
 * `AgentProfile` PDA at the time this vault was initialized. Used by
 * `execute_transfer` and `execute_token_transfer` to re-derive the
 * profile PDA address for the suspension check. Must match the nonce
 * stamped in `AgentProfile.registration_nonce`.
 */
profileNonce: number | bigint; 
/**
 * AUD-023 / PR-X: Unix timestamp of the most recent
 * `update_agent_identity` rotation. Used to enforce a sliding-window
 * rotation cap of one rotation per 24h, preventing a compromised
 * authority from rotating to a fresh hot key, draining the daily cap,
 * and rotating again to bypass the daily limit.
 *
 * Initialized to 0 by `initialize_vault`, so the very first rotation
 * always succeeds. Updated to `Clock::get()?.unix_timestamp` on every
 * successful rotation.
 *
 * Migration note: vaults deployed before PR-X have this field
 * implicitly set to 0 (Anchor zero-fills new fields at the end of the
 * account on first deserialization after the upgrade). Their first
 * rotation post-upgrade is therefore unrestricted; subsequent rotations
 * are gated by the 24h window. Bumps the account `space` by 8 bytes.
 */
lastRotationAt: number | bigint; 
/**
 * ADR-138: monotonically-increasing counter incremented on every
 * successful `update_policy` call. Stamped into every
 * `ExecutionAttested` event so off-chain consumers can pin which
 * policy version was in force when a value-moving action executed.
 *
 * Initialized to 0 by `initialize_vault`. Bumped via
 * `checked_add(1)` in `update_policy`; the protocol does NOT support
 * rollback, so the field is strictly increasing across the vault's
 * lifetime.
 *
 * Migration note: vaults deployed before ADR-138 have this field
 * implicitly set to 0 on first post-upgrade deserialization (Anchor
 * zero-fills new trailing fields). Their first `update_policy` after
 * the upgrade lands at version 1; subsequent actions are stamped
 * with whatever the cumulative version is. Bumps the account `space`
 * by 4 bytes.
 */
policyVersion: number; 
/**
 * ADR-111: Count of `DelegationGrant` PDAs currently outstanding for
 * this vault. Bumped on `create_delegation_grant`, decremented on
 * `revoke_delegation_grant`. The vault rejects creation of a new
 * grant once this reaches `MAX_ACTIVE_GRANTS_PER_VAULT` (32).
 *
 * Migration note: vaults deployed before ADR-111 have this field
 * implicitly set to 0 (Anchor zero-fills new fields appended at the
 * end of the account on first post-upgrade deserialization). Bumps
 * the account `space` by 1 byte; the existing 200-byte safety margin
 * in the vault allocation absorbs it without a layout change.
 *
 * Ordering note: this field MUST stay after `policy_version` (ADR-138)
 * so the Anchor zero-fill semantics work for vaults deployed with the
 * ADR-138 upgrade but not yet the ADR-111 one. New trailing fields go
 * at the end of the struct in chronological merge order.
 */
activeGrantCount: number;  };

/** Gets the encoder for {@link VaultArgs} account data. */
export function getVaultEncoder(): Encoder<VaultArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['agentIdentity', getAddressEncoder()], ['authority', getAddressEncoder()], ['paused', getBooleanEncoder()], ['spentTodayLamports', getU64Encoder()], ['lastSpendDay', getU64Encoder()], ['policy', getVaultPolicyEncoder()], ['txsInCurrentWindow', getU32Encoder()], ['rateLimitWindowStart', getI64Encoder()], ['tokenSpendRecords', getArrayEncoder(getTokenSpendRecordEncoder())], ['bump', getU8Encoder()], ['profileNonce', getU64Encoder()], ['lastRotationAt', getI64Encoder()], ['policyVersion', getU32Encoder()], ['activeGrantCount', getU8Encoder()]]), (value) => ({ ...value, discriminator: VAULT_DISCRIMINATOR }));
}

/** Gets the decoder for {@link Vault} account data. */
export function getVaultDecoder(): Decoder<Vault> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['agentIdentity', getAddressDecoder()], ['authority', getAddressDecoder()], ['paused', getBooleanDecoder()], ['spentTodayLamports', getU64Decoder()], ['lastSpendDay', getU64Decoder()], ['policy', getVaultPolicyDecoder()], ['txsInCurrentWindow', getU32Decoder()], ['rateLimitWindowStart', getI64Decoder()], ['tokenSpendRecords', getArrayDecoder(getTokenSpendRecordDecoder())], ['bump', getU8Decoder()], ['profileNonce', getU64Decoder()], ['lastRotationAt', getI64Decoder()], ['policyVersion', getU32Decoder()], ['activeGrantCount', getU8Decoder()]]);
}

/** Gets the codec for {@link Vault} account data. */
export function getVaultCodec(): Codec<VaultArgs, Vault> {
    return combineCodec(getVaultEncoder(), getVaultDecoder());
}

export function decodeVault<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<Vault, TAddress>;
export function decodeVault<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<Vault, TAddress> | MaybeAccount<Vault, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getVaultDecoder());
}

export async function fetchVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Vault, TAddress>> {
  const maybeAccount = await fetchMaybeVault(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVault<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Vault, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVault(maybeAccount);
}

export async function fetchAllVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Vault>[]> {
  const maybeAccounts = await fetchAllMaybeVault(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVault(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Vault>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeVault(maybeAccount));
}