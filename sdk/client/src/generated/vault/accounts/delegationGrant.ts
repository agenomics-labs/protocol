/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, type Account, type Address, type Codec, type Decoder, type EncodedAccount, type Encoder, type FetchAccountConfig, type FetchAccountsConfig, type MaybeAccount, type MaybeEncodedAccount, type ReadonlyUint8Array } from '@solana/kit';
import { getGrantTokenCapDecoder, getGrantTokenCapEncoder, type GrantTokenCap, type GrantTokenCapArgs } from '../types/index.js';

export const DELEGATION_GRANT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([17, 243, 30, 117, 106, 128, 57, 128]);

export function getDelegationGrantDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(DELEGATION_GRANT_DISCRIMINATOR); }

export type DelegationGrant = { discriminator: ReadonlyUint8Array; 
/**
 * Parent `Vault` PDA. All grant-scoped checks resolve against this
 * vault's policy / pause / suspension gates.
 */
vault: Address; 
/**
 * The vault `authority` at grant creation. Pinned so a subsequent
 * authority rotation (none today — but reserved for governance) can't
 * re-attribute existing grants.
 */
grantor: Address; 
/** The grantee hot key — required signer for `execute_grant_*`. */
grantee: Address; 
/** Bitmap of authorized actions (see `grant_actions::*`). */
allowedActions: number; 
/** Cumulative lifetime SOL spend cap, in lamports. */
spendCapLamports: bigint; 
/** Cumulative lifetime SOL spent under this grant, in lamports. */
spentLamports: bigint; 
/**
 * Per-mint SOL/SPL spend caps. Each entry is independent of the
 * vault's `token_spend_records`. Empty = no SPL transfers allowed
 * via this grant regardless of `allowed_actions` —
 * `execute_grant_token_transfer` MUST find a matching cap or fail.
 */
tokenSpendCaps: Array<GrantTokenCap>; 
/**
 * Optional list of allowed recipient pubkeys. Empty list means
 * "any recipient" subject to the vault's own recipient/program
 * guards. Length bounded by `MAX_GRANT_ALLOWED_RECIPIENTS`.
 */
allowedRecipients: Array<Address>; 
/**
 * Unix-seconds expiry. `0` is the "no expiry" sentinel. Past-tense
 * expiry forces every `execute_grant_*` to fail with
 * `GrantExpired`. Updates can only ratchet this DOWN (see
 * `update_delegation_grant` invariant).
 */
expiresAt: bigint; 
/**
 * `true` once `revoke_delegation_grant` has fired. Set is permanent
 * for the lifetime of the account.
 */
revoked: boolean; 
/**
 * Unix-seconds of grant creation. Pinned at create time; never
 * mutated.
 */
createdAt: bigint; 
/**
 * PDA nonce. Used as the third seed so a single vault/grantee pair
 * can hold multiple grants over its lifetime — e.g. a revoked grant
 * with `nonce = 0` and a fresh grant at `nonce = 1`. Nonce reuse
 * against an open PDA is impossible (Anchor's `init` constraint
 * guards it); nonce reuse against a closed PDA is documented as a
 * future risk in ADR-111 §"Security" — close is deferred to v2.
 */
nonce: number; 
/** PDA bump cached so the runtime doesn't re-derive on every read. */
bump: number;  };

export type DelegationGrantArgs = { 
/**
 * Parent `Vault` PDA. All grant-scoped checks resolve against this
 * vault's policy / pause / suspension gates.
 */
vault: Address; 
/**
 * The vault `authority` at grant creation. Pinned so a subsequent
 * authority rotation (none today — but reserved for governance) can't
 * re-attribute existing grants.
 */
grantor: Address; 
/** The grantee hot key — required signer for `execute_grant_*`. */
grantee: Address; 
/** Bitmap of authorized actions (see `grant_actions::*`). */
allowedActions: number; 
/** Cumulative lifetime SOL spend cap, in lamports. */
spendCapLamports: number | bigint; 
/** Cumulative lifetime SOL spent under this grant, in lamports. */
spentLamports: number | bigint; 
/**
 * Per-mint SOL/SPL spend caps. Each entry is independent of the
 * vault's `token_spend_records`. Empty = no SPL transfers allowed
 * via this grant regardless of `allowed_actions` —
 * `execute_grant_token_transfer` MUST find a matching cap or fail.
 */
tokenSpendCaps: Array<GrantTokenCapArgs>; 
/**
 * Optional list of allowed recipient pubkeys. Empty list means
 * "any recipient" subject to the vault's own recipient/program
 * guards. Length bounded by `MAX_GRANT_ALLOWED_RECIPIENTS`.
 */
allowedRecipients: Array<Address>; 
/**
 * Unix-seconds expiry. `0` is the "no expiry" sentinel. Past-tense
 * expiry forces every `execute_grant_*` to fail with
 * `GrantExpired`. Updates can only ratchet this DOWN (see
 * `update_delegation_grant` invariant).
 */
expiresAt: number | bigint; 
/**
 * `true` once `revoke_delegation_grant` has fired. Set is permanent
 * for the lifetime of the account.
 */
revoked: boolean; 
/**
 * Unix-seconds of grant creation. Pinned at create time; never
 * mutated.
 */
createdAt: number | bigint; 
/**
 * PDA nonce. Used as the third seed so a single vault/grantee pair
 * can hold multiple grants over its lifetime — e.g. a revoked grant
 * with `nonce = 0` and a fresh grant at `nonce = 1`. Nonce reuse
 * against an open PDA is impossible (Anchor's `init` constraint
 * guards it); nonce reuse against a closed PDA is documented as a
 * future risk in ADR-111 §"Security" — close is deferred to v2.
 */
nonce: number; 
/** PDA bump cached so the runtime doesn't re-derive on every read. */
bump: number;  };

/** Gets the encoder for {@link DelegationGrantArgs} account data. */
export function getDelegationGrantEncoder(): Encoder<DelegationGrantArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['vault', getAddressEncoder()], ['grantor', getAddressEncoder()], ['grantee', getAddressEncoder()], ['allowedActions', getU8Encoder()], ['spendCapLamports', getU64Encoder()], ['spentLamports', getU64Encoder()], ['tokenSpendCaps', getArrayEncoder(getGrantTokenCapEncoder())], ['allowedRecipients', getArrayEncoder(getAddressEncoder())], ['expiresAt', getI64Encoder()], ['revoked', getBooleanEncoder()], ['createdAt', getI64Encoder()], ['nonce', getU8Encoder()], ['bump', getU8Encoder()]]), (value) => ({ ...value, discriminator: DELEGATION_GRANT_DISCRIMINATOR }));
}

/** Gets the decoder for {@link DelegationGrant} account data. */
export function getDelegationGrantDecoder(): Decoder<DelegationGrant> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['vault', getAddressDecoder()], ['grantor', getAddressDecoder()], ['grantee', getAddressDecoder()], ['allowedActions', getU8Decoder()], ['spendCapLamports', getU64Decoder()], ['spentLamports', getU64Decoder()], ['tokenSpendCaps', getArrayDecoder(getGrantTokenCapDecoder())], ['allowedRecipients', getArrayDecoder(getAddressDecoder())], ['expiresAt', getI64Decoder()], ['revoked', getBooleanDecoder()], ['createdAt', getI64Decoder()], ['nonce', getU8Decoder()], ['bump', getU8Decoder()]]);
}

/** Gets the codec for {@link DelegationGrant} account data. */
export function getDelegationGrantCodec(): Codec<DelegationGrantArgs, DelegationGrant> {
    return combineCodec(getDelegationGrantEncoder(), getDelegationGrantDecoder());
}

export function decodeDelegationGrant<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress>): Account<DelegationGrant, TAddress>;
export function decodeDelegationGrant<TAddress extends string = string>(encodedAccount: MaybeEncodedAccount<TAddress>): MaybeAccount<DelegationGrant, TAddress>;
export function decodeDelegationGrant<TAddress extends string = string>(encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>): Account<DelegationGrant, TAddress> | MaybeAccount<DelegationGrant, TAddress> {
  return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getDelegationGrantDecoder());
}

export async function fetchDelegationGrant<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<DelegationGrant, TAddress>> {
  const maybeAccount = await fetchMaybeDelegationGrant(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeDelegationGrant<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<DelegationGrant, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeDelegationGrant(maybeAccount);
}

export async function fetchAllDelegationGrant(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<DelegationGrant>[]> {
  const maybeAccounts = await fetchAllMaybeDelegationGrant(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeDelegationGrant(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<DelegationGrant>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeDelegationGrant(maybeAccount));
}