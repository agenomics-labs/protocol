/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getI16Decoder, getI16Encoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, type AccountMeta, type AccountSignerMeta, type Address, type FixedSizeCodec, type FixedSizeDecoder, type FixedSizeEncoder, type Instruction, type InstructionWithAccounts, type InstructionWithData, type ReadonlyAccount, type ReadonlySignerAccount, type ReadonlyUint8Array, type TransactionSigner, type WritableAccount } from '@solana/kit';
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, type ResolvedInstructionAccount } from '@solana/program-client-core';
import { findOwnerNoncePda } from '../pdas/index.js';
import { AGENT_REGISTRY_PROGRAM_ADDRESS } from '../programs/index.js';

export const PROPOSE_REPUTATION_DELTA_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([11, 88, 137, 167, 186, 86, 245, 175]);

export function getProposeReputationDeltaDiscriminatorBytes(): ReadonlyUint8Array { return fixEncoderSize(getBytesEncoder(), 8).encode(PROPOSE_REPUTATION_DELTA_DISCRIMINATOR); }

export type ProposeReputationDeltaInstruction<TProgram extends string = typeof AGENT_REGISTRY_PROGRAM_ADDRESS, TAccountOwnerNonce extends string | AccountMeta<string> = string, TAccountAgentProfile extends string | AccountMeta<string> = string, TAccountSettlementAuthority extends string | AccountMeta<string> = string, TAccountAuthority extends string | AccountMeta<string> = string, TRemainingAccounts extends readonly AccountMeta<string>[] = []> =
Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array> & InstructionWithAccounts<[TAccountOwnerNonce extends string ? ReadonlyAccount<TAccountOwnerNonce> : TAccountOwnerNonce, TAccountAgentProfile extends string ? WritableAccount<TAccountAgentProfile> : TAccountAgentProfile, TAccountSettlementAuthority extends string ? ReadonlySignerAccount<TAccountSettlementAuthority> & AccountSignerMeta<TAccountSettlementAuthority> : TAccountSettlementAuthority, TAccountAuthority extends string ? ReadonlyAccount<TAccountAuthority> : TAccountAuthority, ...TRemainingAccounts]>;

export type ProposeReputationDeltaInstructionData = { discriminator: ReadonlyUint8Array; delta: number; reason: number;  };

export type ProposeReputationDeltaInstructionDataArgs = { delta: number; reason: number;  };

export function getProposeReputationDeltaInstructionDataEncoder(): FixedSizeEncoder<ProposeReputationDeltaInstructionDataArgs> {
    return transformEncoder(getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)], ['delta', getI16Encoder()], ['reason', getU8Encoder()]]), (value) => ({ ...value, discriminator: PROPOSE_REPUTATION_DELTA_DISCRIMINATOR }));
}

export function getProposeReputationDeltaInstructionDataDecoder(): FixedSizeDecoder<ProposeReputationDeltaInstructionData> {
    return getStructDecoder([['discriminator', fixDecoderSize(getBytesDecoder(), 8)], ['delta', getI16Decoder()], ['reason', getU8Decoder()]]);
}

export function getProposeReputationDeltaInstructionDataCodec(): FixedSizeCodec<ProposeReputationDeltaInstructionDataArgs, ProposeReputationDeltaInstructionData> {
    return combineCodec(getProposeReputationDeltaInstructionDataEncoder(), getProposeReputationDeltaInstructionDataDecoder());
}

export type ProposeReputationDeltaAsyncInput<TAccountOwnerNonce extends string = string, TAccountAgentProfile extends string = string, TAccountSettlementAuthority extends string = string, TAccountAuthority extends string = string> =  {
  /**
 * ADR-097: Read-only nonce account. Seeded by
 * `[authority.key().as_ref(), b"owner-nonce"]`. The seeds constraint
 * binds the account address to `authority.key()`, providing the
 * cross-account-reuse guard called for in AUD-001.
 */
ownerNonce?: Address<TAccountOwnerNonce>;
agentProfile: Address<TAccountAgentProfile>;
/**
 * seeds::program ensures this PDA was derived by the Settlement program,
 * proving the call originated there (SEC-1 pattern from ADR-068).
 */
settlementAuthority?: TransactionSigner<TAccountSettlementAuthority>;
/**
 * external seed anchor for `owner_nonce` and `agent_profile`, and
 * constrained equal to `agent_profile.authority` via
 * `has_one = authority`. Not deserialized; only `.key()` is read.
 */
authority: Address<TAccountAuthority>;
delta: ProposeReputationDeltaInstructionDataArgs["delta"];
reason: ProposeReputationDeltaInstructionDataArgs["reason"];
}

export async function getProposeReputationDeltaInstructionAsync<TAccountOwnerNonce extends string, TAccountAgentProfile extends string, TAccountSettlementAuthority extends string, TAccountAuthority extends string, TProgramAddress extends Address = typeof AGENT_REGISTRY_PROGRAM_ADDRESS>(input: ProposeReputationDeltaAsyncInput<TAccountOwnerNonce, TAccountAgentProfile, TAccountSettlementAuthority, TAccountAuthority>, config?: { programAddress?: TProgramAddress } ): Promise<ProposeReputationDeltaInstruction<TProgramAddress, TAccountOwnerNonce, TAccountAgentProfile, TAccountSettlementAuthority, TAccountAuthority>> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_REGISTRY_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { ownerNonce: { value: input.ownerNonce ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: true }, settlementAuthority: { value: input.settlementAuthority ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };


// Resolve default values.
if (!accounts.ownerNonce.value) {
accounts.ownerNonce.value = await findOwnerNoncePda({ authority: getAddressFromResolvedInstructionAccount("authority", accounts.authority.value) });
}
if (!accounts.settlementAuthority.value) {
accounts.settlementAuthority.value = await getProgramDerivedAddress({ programAddress: 'AwjdsNvhR2uwPNbU6F2fsYB33VcNGL5XaANdgsyvZDia' as Address<'AwjdsNvhR2uwPNbU6F2fsYB33VcNGL5XaANdgsyvZDia'>, seeds: [getBytesEncoder().encode(new Uint8Array([115, 101, 116, 116, 108, 101, 109, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114, 105, 116, 121]))] });
}

const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("ownerNonce", accounts.ownerNonce), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("settlementAuthority", accounts.settlementAuthority), getAccountMeta("authority", accounts.authority)], data: getProposeReputationDeltaInstructionDataEncoder().encode(args as ProposeReputationDeltaInstructionDataArgs), programAddress } as ProposeReputationDeltaInstruction<TProgramAddress, TAccountOwnerNonce, TAccountAgentProfile, TAccountSettlementAuthority, TAccountAuthority>);
}

export type ProposeReputationDeltaInput<TAccountOwnerNonce extends string = string, TAccountAgentProfile extends string = string, TAccountSettlementAuthority extends string = string, TAccountAuthority extends string = string> =  {
  /**
 * ADR-097: Read-only nonce account. Seeded by
 * `[authority.key().as_ref(), b"owner-nonce"]`. The seeds constraint
 * binds the account address to `authority.key()`, providing the
 * cross-account-reuse guard called for in AUD-001.
 */
ownerNonce: Address<TAccountOwnerNonce>;
agentProfile: Address<TAccountAgentProfile>;
/**
 * seeds::program ensures this PDA was derived by the Settlement program,
 * proving the call originated there (SEC-1 pattern from ADR-068).
 */
settlementAuthority: TransactionSigner<TAccountSettlementAuthority>;
/**
 * external seed anchor for `owner_nonce` and `agent_profile`, and
 * constrained equal to `agent_profile.authority` via
 * `has_one = authority`. Not deserialized; only `.key()` is read.
 */
authority: Address<TAccountAuthority>;
delta: ProposeReputationDeltaInstructionDataArgs["delta"];
reason: ProposeReputationDeltaInstructionDataArgs["reason"];
}

export function getProposeReputationDeltaInstruction<TAccountOwnerNonce extends string, TAccountAgentProfile extends string, TAccountSettlementAuthority extends string, TAccountAuthority extends string, TProgramAddress extends Address = typeof AGENT_REGISTRY_PROGRAM_ADDRESS>(input: ProposeReputationDeltaInput<TAccountOwnerNonce, TAccountAgentProfile, TAccountSettlementAuthority, TAccountAuthority>, config?: { programAddress?: TProgramAddress } ): ProposeReputationDeltaInstruction<TProgramAddress, TAccountOwnerNonce, TAccountAgentProfile, TAccountSettlementAuthority, TAccountAuthority> {
  // Program address.
const programAddress = config?.programAddress ?? AGENT_REGISTRY_PROGRAM_ADDRESS;

 // Original accounts.
const originalAccounts = { ownerNonce: { value: input.ownerNonce ?? null, isWritable: false }, agentProfile: { value: input.agentProfile ?? null, isWritable: true }, settlementAuthority: { value: input.settlementAuthority ?? null, isWritable: false }, authority: { value: input.authority ?? null, isWritable: false } }
const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;


// Original args.
const args = { ...input,  };




const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
return Object.freeze({ accounts: [getAccountMeta("ownerNonce", accounts.ownerNonce), getAccountMeta("agentProfile", accounts.agentProfile), getAccountMeta("settlementAuthority", accounts.settlementAuthority), getAccountMeta("authority", accounts.authority)], data: getProposeReputationDeltaInstructionDataEncoder().encode(args as ProposeReputationDeltaInstructionDataArgs), programAddress } as ProposeReputationDeltaInstruction<TProgramAddress, TAccountOwnerNonce, TAccountAgentProfile, TAccountSettlementAuthority, TAccountAuthority>);
}

export type ParsedProposeReputationDeltaInstruction<TProgram extends string = typeof AGENT_REGISTRY_PROGRAM_ADDRESS, TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[]> = { programAddress: Address<TProgram>;
accounts: {
/**
 * ADR-097: Read-only nonce account. Seeded by
 * `[authority.key().as_ref(), b"owner-nonce"]`. The seeds constraint
 * binds the account address to `authority.key()`, providing the
 * cross-account-reuse guard called for in AUD-001.
 */
ownerNonce: TAccountMetas[0];
agentProfile: TAccountMetas[1];
/**
 * seeds::program ensures this PDA was derived by the Settlement program,
 * proving the call originated there (SEC-1 pattern from ADR-068).
 */
settlementAuthority: TAccountMetas[2];
/**
 * external seed anchor for `owner_nonce` and `agent_profile`, and
 * constrained equal to `agent_profile.authority` via
 * `has_one = authority`. Not deserialized; only `.key()` is read.
 */
authority: TAccountMetas[3];
};
data: ProposeReputationDeltaInstructionData; };

export function parseProposeReputationDeltaInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>): ParsedProposeReputationDeltaInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
  throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, { actualAccountMetas: instruction.accounts.length, expectedAccountMetas: 4 });
}
let accountIndex = 0;
const getNextAccount = () => {
  const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
  accountIndex += 1;
  return accountMeta;
}
  return { programAddress: instruction.programAddress, accounts: { ownerNonce: getNextAccount(), agentProfile: getNextAccount(), settlementAuthority: getNextAccount(), authority: getNextAccount() }, data: getProposeReputationDeltaInstructionDataDecoder().decode(instruction.data) };
}