[workspace]
members = [
    "programs/agent-vault",
    "programs/agent-registry",
    "programs/settlement",
    "programs/cctp-hook",
]
resolver = "2"

[profile.release]
overflow-checks = true
lto = true
codegen-units = 1
opt-level = 3

[profile.release-with-logs]
inherits = "release"


