// Theme override: extends the stock VitePress default theme with the
// agenomics.xyz marketing palette so docs.agenomics.xyz feels like the
// same product. See custom.css for the actual variable overrides.
import DefaultTheme from 'vitepress/theme';
import './custom.css';

export default DefaultTheme;
