[toolchain]
# Updated to stable to support edition2024 transitive dependencies.
# proc-macro2 is pinned to <1.0.95 in Cargo.toml to maintain
# compatibility with anchor-syn 0.30.1's use of Span::source_file().
channel = "stable"
components = ["rustc", "cargo", "rust-std"]
profile = "minimal"
